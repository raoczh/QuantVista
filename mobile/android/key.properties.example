# release 签名配置模板：
#   1. 复制本文件为同目录下的 key.properties（已 gitignore，绝不提交）；
#   2. 按 mobile/README.md「签名」节生成 keystore 后填入真实值。
# storeFile 是相对 android/app/ 的路径（也可用绝对路径指向仓库外的 keystore）。
storeFile=../../quantvista-release.keystore
storePassword=CHANGE_ME
keyAlias=quantvista
keyPassword=CHANGE_ME
