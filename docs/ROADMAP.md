# 项目现状与实现边界

> 本文档原为实施路线图。阶段 0~8 与后续增强批次已**全部完成并入库**（历史流水账见 git log 与各 commit message），
> 现只保留对后续开发有约束力的内容：当前状态、实现边界、防回归要点、未完成项与欠人工验证清单。
> 表结构以 `server/model/*.go`（GORM AutoMigrate）为准；数据源认知见 `DATA_SOURCES.md`；架构与 UI 约定见 `ARCHITECTURE.md`；部署见 `DEPLOYMENT.md`。

## 1. 当前状态（截至 2026-07-17，外部审查修复批：codex 52 项 8 组核验后修 ~38 项，执行结算 l1→l2）

个人自用 AI 股票研究平台，Go(Gin+GORM)+Vue3(Naive UI)，单容器 go:embed 托管前端，生产 MySQL。已具备：

- **账户与设置**：密码 + GitHub OAuth 双登录、账号绑定/解绑、JWT(access+refresh 可吊销/即时失效)、管理员后台（注册开关/GitHub 凭证/用户与配额管理/**新闻采集配置/LLM 回退/LLM 调用审计页**）、用户级 LLM 配置（OpenAI 兼容、**chat_completions 与 responses 双端点类型**、密钥加密、测试连接、**无配置用户回退管理员指定配置**）、偏好（默认市场/风险/推荐筛选默认[股价默认 ≤50 元、可排除创业板/科创板]/黑名单/流动性门槛/提醒与日报开关）。
- **行情与数据**：东财→腾讯→新浪三源主备切换；实时行情/日线（东财前复权 fqt=1）/指数/榜单（涨幅/成交额/换手率/回调[跌幅升序]/低PB[升序]，方向参数支持「不热」来源）/板块/涨跌家数/两市资金流/腾讯免费估值（PE/PB/市值/换手/量比/振幅/涨跌停价）；交易日历回填、日线批量同步、市场情绪快照；**机构观点（P3a）：卖方研报评级与机构调研按需拉取缓存，进详情页与个股 AI 证据链**；**板块数据（M3c+P3b）：行业/概念热力图、板块详情（指数日线/成分股/主力资金历史透传/行业估值聚合表——中位 PE·PB+横截面与时序分位），板块 AI 分析吃 board_valuation/board_flow 两段**。
- **研究工作台**：市场首页看板、个股详情页（日K/估值/五维评分/快捷动作）、自选股（分组/研究阶段漏斗/错过机会复盘）、持仓（盈亏费税精算/风险计划/买入前清单/结构化复盘/组合总览与风控信号/CSV 导入导出）、投资逻辑卡、投资笔记、条件提醒（6 类规则+命中明细状态机+主动推送）、今日待办聚合、模拟交易（虚拟账户/真实行情成交/佣金印花税）、**指数 ETF 交易**（精选指数 ETF 行情清单、复用模拟盘买卖、ETF 免印花税费率）。
- **AI 链路（全链路信任层）**：五模块分析（个股/市场/板块/自选/持仓，反方视角三字段、panel 多角色、变化检测 diff）、个股问答（快照复用多轮、流式输出）、横向对比（可选 AI 点评）、短线/长线推荐（**四阶段流水线**：多源建池→用户筛选硬过滤→本地量化评分→LLM 仅对 Top10 精选/否决）、推荐追踪（止盈止损盘中触达判定/基准 alpha/7/14/30 日节点收益/历史表现统计/复盘待办已读消项）、收盘日报（复盘+明日推荐+卖点提醒自动建规则，支持删除重生成）、**AI 白话建策略（P3c）**：自然语言→条件树解析（factorDefs 程序生成因子字典、unmatched 兜底禁硬凑、人话预览+用户确认才落编辑器，解析 prompt sp1）。**推荐/日报生成为异步任务**（processing 秒回+前端轮询，后台独立 Context 不受浏览器/反代超时影响；AI 超时/网络类失败按量化排名生成降级推荐——ATR 规则价位+`degraded_source` 透明标记+置信度恒 low）。**全部 AI 产出标注所用 LLM 配置名与模型**（日报落库三列，对比/选股解析随响应回传，前端 useLlmLabel 共享映射）。
- **信任机制（推荐域已全量，其余 AI 模块同标准推广）**：程序化证据核验（LLM 引用数字与数据快照容差比对）、程序合成置信度（量化排名×核验吻合率×数据完备度，不信 LLM 口头置信度）、AI 复核员（可选二次调用挑刺 pass/warn/reject，reject 强制降级）、候选池全景透明面板、prompt 强制引用数值+禁先验记忆+允许拒选、数据快照落库可复现。**推荐准确性工程（S0~S2 已落地，规划 `docs/RECOMMENDATION_ACCURACY_PLAN.md`）**：标签事实表+反事实事件表（统一执行模拟结算 1/5/10/20/60 日净收益/alpha/三重障碍，影子标签对照）、regime 三档大盘闸门（影子）、目标波动仓位建议、名单去相关、错误归因报表；**S2 影子件**：反方研究员（对每只 buy 独立构建最强 bear case，只展示不改写，high 危记影子事件）、数据质量门控（按策略分场景的缺失面→would-be 置信度封顶，只记录不执行，情绪缺失≠中性区分标记）、影子门控对照报表（gated vs ungated 成熟收益+覆盖率，一切门控转正评审的数据地基）、反思记忆表结构（LLM 反思延后至成熟样本 ≥30）；**S3 测量层（纯测量零门控零 LLM）**：每日因子快照（fv1 首写胜不可变）、候选池召回评估（Recall@K/来源消融/错失机会率）、因子 RankIC 排行（19 个 A 类无量纲因子 as-of 重建）、walk-forward 评估基线（训练/验证/测试滚动切分 Purge+Embargo、手工评分 baseline、Precision_net@K 与 Precision_alpha@K/净收益中位数/严重亏损率、评分 Top10 组合月度走查，管理端只读页）。

## 2. 当前实现边界（承认的简化，非隐藏问题）

- **市场范围**：数据适配器只支持 A 股 `cn`（仅沪深股票 + 沪深 ETF/LOF 基金，不含北交所 4/8 开头）；美股/港股需接数据源后再开放。
- **AI 分析结构**：结构化字段为 `rating/confidence/summary/highlights/risks/opportunities/suggestions/anti_thesis/kill_switches/unknowns/disclaimer`；更完整的 `market_context/data_points` 等属目标态，扩字段需同步 schema/前端/历史兼容。
- **追踪复权**：`daily_bars` 主源为东财**前复权**（fqt=1，最新价重锚），除权后历史整段重刷、与生成时点 RefPrice 快照价错位（note 已标注）；新浪兜底无复权参数、无成交额（落库不覆盖 amount），两源口径不保证一致。彻底解决需 `corporate_actions` 复权因子表（未建）。
- **未建的表**：`corporate_actions`、`audit_logs`（操作审计，个人自用降级；LLM 调用审计 `llm_call_logs` 已于 2026-07 杂项批落地，90 天自动清理）。
- **配额**：次数制终身累计（`action_limit/action_used` 熔断：一次手动动作=1 次、repair/panel 等内部多轮不重复计、后台任务不计次、token 仅审计），无自动周期重置；管理员可 `PUT /api/admin/users/:id/quota` 调上限/手工清零。
- **LLM**：单一默认配置（无分析/推荐双默认）；仅 OpenAI 兼容 provider（`endpoint_type` 支持 chat_completions 与 responses 双端点）；问答页支持流式渲染，其余模块对外整包返回（内部请求流式优先、上游拒绝时自动回落非流式，审计按实际形态记录）；未配置用户回退管理后台指定配置（总闸可关）。
- **stock_quotes** 为 symbol+market 唯一的最新快照单行覆盖（非历史序列）；`daily_bars` 用 symbol+market 自然键、无 adj_close。
- **短线追踪状态**枚举 `active/take_profit/stop_loss/expired/tracking/no_data`，无用户买卖联动（watching/closed 未实现）。
- **股票评分**为五维技术面（趋势/动量/位置/量能[已融合主力资金分]/风险）；估值/财务维度未并入评分（F2 财务数据已接，只进长线推荐加分与 AI 证据，评分维度按需再扩）。
- **宏观数据未接**：分析/问答/对比/推荐均已要求模型不得虚构未提供数据。新闻情绪（N2）/财务详情（F2）/财报日历（F1）已由东财+财联社免 token 路线实现，Tushare 至今未成为任何功能的前置依赖。
- **短线计划**：prompt 与后处理已含 A 股 T+1、涨跌停、100 股一手、交易日有效期约束；未做完整订单可执行性模拟。
- **ETF**：仅接入沪深场内基金行情/日线与模拟盘交易；ETF 不参与个股推荐候选池（涨停幅度按股票板块前缀推断对 ETF 不适用，已透明排除）；ETF 无 PE/PB 个股估值（分析快照已标注不适用）。

## 3. 推荐流水线与信任层防回归要点（改代码前先读）

- **parseAndFilterPicks 空数组=合法拒选**（`*[]recPick` 指针语义区分「缺 picks 字段」与「显式空数组」），别改回「空=报错」——p4 prompt 明示宁缺毋滥可 0 只，空数组触发 repair 会强迫模型硬凑标的。
- **scorePool 日线拉取失败=透明排除**，不能让无日线标的按中性 50 分参与排名（会挤占 Top12 且绕过追高保护）。
- **池满补位只补一轮**（poolFullPrefix 前缀识别），拉取总量有界；`maxScanCandidates=48 / maxLLMCandidates=10 / maxPoolIntake=240 / poolSnapshotMax=150` 四个常量各司其职（2026-07-14 maxLLMCandidates 16→10：上游 60s 整包超时下压 LLM 输入与落选理由输出量）。
- **换手率两级阈值**（2026-07-06 分位化，替代旧的 20% 一刀切）：>30%（deadTurnoverHardPct）阶段②无条件硬拦；20~30% 放行到阶段③按 pos_60 分档——≥65% 高位判「死亡换手」排除，低位保留但评分扣 5 分并标注风险。用户筛选区间必须被钳在 30 之内（UI :max=30），「钳制上限=绝对硬顶」不变式防空池死局；别改回一刀切，否则换手率榜来源几乎被清空（该榜前排常年 20%+）。
- **榜单来源随策略组合**（strategySources）：升序榜只有低PB可用并必须带行级 keep（滤负 PB+亏损/高PE 价值陷阱）；**「回调票」严禁用跌幅榜升序取**——全市场跌超 2% 家数常年远大于 100，升序前 100 永远是深跌段（2026-07-06 实测 [-21%,-7.6%]），「温和回调」必须从成交额/换手深捞结果里 keep（ChangePct∈[-7,-0.5]）；换手路深捞 100 后取温和带（3~15%），榜单前排极端换手大多活不过硬拦。北交所（43/83/87/920 前缀）在 candidateEligible 排除（数据源不支持，日线必失败、白占评分名额）。新浪榜单自带 pe/pb/nmc（万元）仅作行级过滤与腾讯估值缺失时的兜底（PB/FloatCap 腾讯 >0 才覆盖），腾讯口径优先。
- **评分名额按来源轮转**（assignScanQuota）：自选整组优先、其余来源逐轮各出一只——别改回按池序先到先得（第一路榜单 50+ 只会垄断整个量化窗口，其余来源全部沦为「池满」）。换手下限钳 25（非 30）：给 [min,30] 留可行带。长线「PE 为负（亏损）」扣分条件是严格 `PETTM < 0`——PETTM==0 是估值缺失而非亏损，新浪 PB 兜底存在后 `<=0 && PB>0` 会把缺失误标成假证据（信任层自伤）。Pos60 取尾部 60 根（bars 全长 90，全窗遍历口径会漂成 90 日，而它是高位换手排除的判定依据）。
- **LLM 只见量化 Top10 子集 map**（poolBySymbol），parseAndFilterPicks 以它做反编造校验——池内但非 Top10 的标的同样会被丢弃，这是有意设计。
- **verifyEvidence 的 extra 变参契约**：调用方必须把「模型自己输出的计划价」与「用户设定阈值」并入合法值域，否则模型合法复述自己的结论会被误标为幻觉（信任层自伤）；无小数点整数跳过规则（≤99 的 rank 类/年份/六位代码/噪声整数）同理不可删。
- **复核 reject 必须级联**：强制 Action=watch 且置信度压 ≤25，否则「复核否决」徽章与高置信度并排自相矛盾；复核 best-effort 失败不阻断主结果，但 token 必须累加进同一批次并 consumeQuota。
- **LLM 输出的整数字段一律用 FlexInt**（模型会输出 72.5/"80"，裸 int 让整个 JSON 反序列化失败被静默丢弃）。
- 前端 `STRATEGY_NAME` 静态字典是历史批次标题（"value"）bug 的兜底，别删；新记录走后端固化的 `batch.title`。
- 日报明日推荐与手动推荐完全同链路（GenerateAuto→generate），筛选偏好 `rec_filters_json` 对两者同时生效；自动链路不触发 AI 复核、不计次数配额。
- prompt/策略改动须递增版本号（分析 `analysisPromptVersion`、推荐 prompt/strategy 版本），保证历史记录可复现可归因。
- **新闻采集（N1）上游口径**（2026-07-06 实测）：财联社只有 `/v1/roll/get_roll_list` 活着，`sign = md5hex(sha1hex(按参数名字典序的 querystring))`，老 nodeapi 已死；东财快讯 `req_trace`（uuid hex）缺失返回空，`stockList` 是**字符串数组**（"1.688035"/"90.BK1175"，别按对象数组解析）；东财个股新闻 search-api 标准 Go client 实测**可用**（无需 utls，但保留「单只失败整轮降级」逻辑防日后被指纹拦截）。去重 Dice 阈值 0.85 是「尾部增删判重、改动较大放行」的实测锚点，调低会误杀无关新闻。
- **新闻→AI 信号（N2）防回归**（2026-07-07）：①`News.Sentiment` 空串=未增强、增强后恒为 positive/negative/neutral——增强失败也要走规则兜底落枚举值，别留空串（会被每轮重挑反复烧 token）；②两个幂等键分清：逐条增强按 news.id、个股聚合情绪分按 (symbol,date) 唯一索引+包级 mutex+map 合并（stock_sentiments 一天一行，当日后来的新闻不改已落库分——有意设计，保证推荐批次内证据可复现）；③LLM 增强挂**首个管理员默认配置**、consumeQuota(adminID, tokens, false) 只记审计不扣次；成本分级（P1/P2 全量、P3 规则先行缺板块才简化 LLM 且每轮≤10 条）别拉平；④related_sectors 必须过 `newsSectorWhitelist` 白名单（防幻觉板块），LLM 输出方向与分数矛盾时以方向为准分数归 0；⑤新闻标题是「文本型合法来源」：分析 fillAnalysisTrust / 问答 Ask / 日报 dailyReviewEvidence 三处都已把标题 decimalNumbersIn 并入核验值域，新增消费方照此办理（否则忠实引用标题里的数字会被误报幻觉）；⑥日报事件段 4 步硬规则在 `newsevent.go`（LLM 只写 events_review 摘要），黑名单/打分/合并/TopN 都是纯函数有单测，调阈值先跑 `TestSelectReportEvents*`；⑦推荐 senti 因子只在 SentiNews>0 时动分（无新闻≠中性 0 分证据），senti_score 已进 candidateValueSet 核验值域；本批版本号：分析 p6、问答 q4、推荐 p6/s4。
- **财报日历与公告（F1）防回归**（2026-07-07）：①所有 RPT_* 报表必须走 `datasource/emdatacenter.go` 的 DataCenterQuery 迭代器（全局令牌桶 QPS≤2 是**包级共享**，绕开直连会破坏限流）；上游「无数据」返回 success=false+code=9201，客户端已归一为 ErrNoData——增量刷新空轮是正常态，别当错误重试；②三类财报表唯一键 (symbol,market,report_date)、upsert 幂等（业绩预告会修正、预约披露有三次变更，重复拉取靠 DoUpdates 覆盖）；增量游标存 options（"period|date"），报告期切换自动全量、NOTICE_DATE 过滤带 1 天重叠防同日晚间发布漏采；预约披露无 NOTICE_DATE 走每日全量重拉（一期约 12 页，别改成增量）；`reportPeriodsAsOf` 返回**最近两期**是为覆盖 1~4 月年报+一季报并行披露季，别省成一期；③提醒 kind `earn_date`/`earn_fcst` ≤16 字符（列宽 size:16）；盘中 15min 行情评估（StartAlertJobs→evaluateUserMarket）的规则查询**显式 kind NOT IN 排除财报类**（否则每 symbol 拉行情空转），财报类由 finance job 每日 19:05 数据刷新后 EvaluateEarningsAll 一评；手动「立即检查」EvaluateUser 两类都评（财报类查本地表零上游成本）；防重靠 TriggeredAt 与披露窗口/预告 NoticeDate 比较（纯函数 evaluateEarnDate/evaluateEarnFcst 有单测），别改成只靠同日去重（Once=false 会连续多日重复提醒）；④公告 (symbol,art_code) 唯一冲突忽略即增量；详情页按需补拉有 1h 进程内冷却（annFetchAllowed），别删（防详情页被刷打上游）；⑤公告标题进 AI 证据池：个股快照 announcements 块 + `announcementTitleTexts` 已并入分析/问答两处核验值域（同新闻标题前例）；本批版本号：分析 p7、问答 q5。
- **技术指标库与筹码分布（T1）防回归**（2026-07-07）：①指标口径（`service/indicator.go` 头注释是权威）：RSI/ATR 用 **Wilder 平滑 α=1/n、seed=首值**（通达信 SMA(X,N,1) 递推），别改回参考项目的滚动 SMA 口径；MACD 柱=**2×(DIF−DEA)** A 股口径别改 1 倍；EMA seed=首值（pandas ewm adjust=False）；BOLL 样本标准差（n-1）。递推指标数值受输入窗口起点影响：详情页 API 加 100 根 warmup 后截尾、推荐因子吃 210 根全长，两处允许 <1% 微差，属固有性质非 bug；②`scorePool` 拉日线已从 90 改为 **chipBarLimit=210**（同一上游请求仅行数差异）：五维评分**必须截尾部 factorBarLimit=90 再喂 computeScore**——positionScore 是全窗口径，直接喂 210 根会把「90 日区间位置」漂成 210 日（Pos60 同类前科）；computeCandFactors 可吃全长（内部全部尾窗/递推，不漂移）；③五维升级：动量维=涨幅合成 60%+RSI **凹形** 40%（55~70 满分、≥70 陡降是刻意的反追高逻辑，别改成「RSI 越大分越高」的线性）；风险维=回撤反向 60%+ATR% 反向 40%；样本不足 rsiMinBars/atrMinBars 时自动退回纯旧口径，不臆测；④筹码（`service/chip.go`）：<120 根拒算、<210 根标 data_limited；**推荐加分门槛 ChipBars≥210**（次新股累积失真不给分）；换手率优先日线自带（东财 f61），缺失根按股本推断（有换手根的中位数反推，全缺失再用腾讯 FloatCap/现价），推断也不可得=拒算而非拍默认值；停牌/零成交 t=0 不衰减不新增；⑤喂给 LLM 的新因子字段（rsi_14/macd_*/boll_*/atr_*/chip_profit/chip_avg_cost）已同步进 `candidateValueSet` 核验值域——日后再加因子字段必须同步，否则忠实引用被误报幻觉（信任层自伤）；⑥`datasource.Bar.TurnoverRate` 只有东财日线有（f61），新浪兜底恒 0；`persistDailyBars` 在本批换手全 0 时不覆盖已写真值（amount 同款防回退模式）；daily_bars 新增 turnover_rate 列；⑦详情页副图：indicators 与 bars 是两次独立请求，前端 `alignByDate` 按 trade_date 对齐，别改成按下标 zip（末根可能差一天）；本批版本号：推荐 p7/s5。
- **稳定性与流式体验（S1）防回归**（2026-07-07）：①东财断路器（`datasource/breaker.go`）：push2 族限流（EOF/断连/302/403/429/502）自动降级 `push2delay.eastmoney.com` 并**实例级记住不回切**；push2his/push2ex **无备用域**（push2dhis 不可用，别加），连续 5 次限流熔断 2 分钟快速失败；ctx 取消/超时不计入熔断计数；非 push2 族（datacenter/公告）不经断路器（自有限流治理）。适配器 `fetch` 字段可注入假实现，改断路器逻辑先跑 `TestBreaker*`。②健康滑窗（`datasource/health.go`+manager routeCap）：每 (源,能力) 50 次环形窗，empty>50% 或 error>30%（样本≥20）冷却 300s 踢出轮询、**触发即清窗**（恢复后从零观察）；**全部源冷却时有补跑轮兜底**，别改成直接报错；ErrNotSupported/ErrSymbolInvalid 不记滑窗（非健康问题）；两层超时=总预算 15s（仅调用方无 deadline 时兜底）+单源 6s（须小于 doGet 2×8s 最坏耗时）；错误归一 {EMPTY/UPSTREAM_TIMEOUT/PARSE_ERROR/UPSTREAM_ERROR} 记 DEBUG；健康端点 `GET /api/admin/datasources`；**DataSourceConfig 死表已删**（模型+迁移注册，旧库物理表可手工 DROP）。③风险闸门（`service/riskgate.go`）：block（ST/退市）/warn（一字板=|涨跌幅|≥9.5 且振幅<1、成交额<3000 万）/info（市值<30 亿）随个股快照 risk_gate 块注入分析/问答 prompt 并透传前端（AnalysisView/QaConversationView 的 risk_flags）；**flags 为空也注入 note**（「质押/解禁未接入请自行核查」措辞纪律恒在）；**riskGateTexts 已并入分析/问答核验值域**（提示文本里的 9.5/3000 等阈值数字是合法来源，删了会误报幻觉）；持仓模块 capital_context 只在偏好 total_capital>0 时注入，割/守/补三选一是 prompt 硬要求（禁骑墙）；版本 p8/q6。④问答流式：`chatCompletionStream`（SSE 剥 data:/[DONE]/finish_reason 终止/坏 chunk 容错跳过/**流中断即失败不落半截回答**）；Ask 与 AskStream 共用 prepareAsk/finalizeAsk（快照/核验/落库口径必须一致，改一处两处同步）；`LLMConfig.Stream=false` 时 AskStream 退回非流式整段吐出；NDJSON 协议 {module,code,chunk,status}（**code 字段为批量场景预留、单标的恒空**）+`X-Accel-Buffering: no`；前端 fetch+getReader 行缓冲、100ms 节流渲染、**LLM 输出 v-html 前必须过 `lib/markdown.ts` 的 renderMarkdown（marked+DOMPurify 白名单，链接/图片已剥）**；核验徽章靠 done 行整体替换会话后置出现。
- **财务数据与估值增强（F2）防回归**（2026-07-07）：①F10 主要财务指标走 `datasource/emfinance.go` 的 GetF10MainFinance——base URL 是 `datacenter.eastmoney.com/securities/api/data/get`（type/sty/p/ps 参数族，与 datacenter-web v1 的 reportName 参数族不同），但**共享包级令牌桶 dcThrottle（QPS≤2）与重试纪律**，别绕开直连；三大报表 emweb 三表 AjaxNew 先 lrbDateAjaxNew 试探 companyType 4→3→2→1（不匹配返回无 data 空对象非错误），dates 5 期一批防 URL 过长；②按需拉取+缓存：7 天新鲜期（按表内 MAX(updated_at) 判）+1h 尝试冷却（成功失败都记，**包级共享 finSyncTry**——FinanceService 有多实例，别改成实例字段），触发点只有 详情页财务块/个股 AI 快照（仅 F10）/长线推荐（预算 finRecFetchBudget=12 只/次），**严禁全市场普查**；③GORM 列名坑：字段名含 YoY 会被转成 yo_y（revenue_yo_y）——新表 FinanceIndicator 用显式 `gorm:"column:revenue_yoy"`，**F1 旧表 EarningsExpress 保持物理列 revenue_yo_y**（AssignmentColumns 已按物理列名修正，改回 revenue_yoy 会让快报修正永远覆盖不进去），AutoMigrate 不改已有列名；④值域同步铁律（T1 前例）：长线名单 fin 字段（ROE/增速/毛利率/净利率/负债率）已进 candidateValueSet；分析/问答的 finance 段是 JSON 数值叶子由 snapshotValueSet 自动收集，无需手工；日后给 fin/finance 加字段必须同步；⑤财务加分只在 c.Fin 非 nil 时动分（缺失不惩罚是明文设计），业绩恶化扣分（净利同比≤-30）是长线通用项；⑥上游 null 落 0：消费方按「0 可能=缺失」处理，prompt 已声明不得据 0 下「归零」结论；本批版本号：推荐 p8/s6、分析 p9、问答 q7。
- **全市场日线地基（M1 第一部分）防回归**（2026-07-08）：①clist 全市场快照（`datasource/eastmoney_clist.go`）：上游把 **pz 硬钳制在 100**（实测 pz=6000 只回 100 行），必须 56 页翻页；**fid=f12 代码升序翻页**（涨跌幅序盘中排名漂移会重复/漏行，别改回 fid=f3）；fs 四段=沪深 A 股不含北交所（cnSecid 不识别，纳入前先扩 secid 映射）；**半截快照必须整轮拒绝**（<total 90% 报错——部分落库会留"当日只有一半股票有 bar"的静默缺口）；停牌行价格字段是 "-"（emNum 容错为 0）但 f18 昨收有值、行保留进宇宙字典；走 `e.get`（push2 断路器）。②宇宙字典 `market_sync_states` **有意不并入 stocks 表**——stocks 是"用户关心的标的"语义，SyncTrackedDailyBars 按 800 上限轮转同步它，灌入全市场 5500 只会稀释轮转；两条链路独立。③全市场链路（增量/初始化/重锚）**强制东财直连（wideSource），不走 mgr 路由**——新浪日线不复权，兜底写库会把前复权基准锚坏；直连 bars 须手动补 Source="eastmoney"（manager 层才自动填）。④除权双层检测：persistDailyBars 的 detectAndRebase **均匀采样 60 点多点比对**（只取尾部会漏"窗口外旧基准"断层——这就是它要防的"部分窗口重写"漏检）；**比对锚必须排除"今天"的根**（当日 close 盘中持续变化，会把盘中波动误判成除权，每天海量误重锚）；仅 Source=="eastmoney" 才检测（新浪不复权必偏差）；盘后 SyncMarketWide 的 f18 昨收初筛只兜底无人访问的标的；容差 0.5%（rebaseTolerance，更小的分红除权会漏检但量级可忽略，是承认的简化）；单轮重锚上限 200 只（超过=上游口径整体漂移，拒绝批量重写）。⑤重锚=**事务内整股删+插 250 根**（窗口外更老数据无法对齐新基准，留着是毒数据；因子/筹码/回测窗口均 ≤250 日）+记 adjust_epoch；重锚失败退回写本窗并**把 states 踢回 pending** 交初始化任务自愈（那条路拉全量 250 根多点采样必再抓住断层）；rebaseInflight 防并发重复重拉。⑥历史初始化**源故障熔断**：连续 10 只网络类失败（非 ErrNoData）判源限流，本轮中止且**不把失败记到标的头上**（push2his 限流期逐只硬扫会把全宇宙误标 failed 还加速打上游）；ErrNoData/ErrSymbolInvalid 是标的问题直接记，fail_count 达 3 置 failed 不再骚扰（退市整理/长停股）；断点=表状态，游标只向前（防失败行同轮反复取）。⑦f124 定 trade_date（max 时间戳的日期），全缺失整轮报错不猜日期；周末手动跑落的是上一交易日快照，幂等无害。
- **因子宽表与条件树选股（M1 第二部分）防回归**（2026-07-08）：①宽表因子口径纪律（`service/factortable.go`）：computeWideRow **直接复用 indicator.go 底层序列函数（rsiSeries/macdSeries/bollSeries/atrSeries）与最小样本常量、窗口因子照 recfactor.go 尾窗口径**，`TestComputeWideRowParity` 与 computeCandFactors 对拍锁死——改任一侧口径对拍必炸，两处同步改；atr_14 存 **round2**（与 computeIndicatorSnapshot 对齐），别顺手改 round3。②缺失=**NaN 语义**：样本不足/筹码拒算的因子列是 NaN，条件求值 NaN 恒 false，**布尔因子 NaN 连 is_false 也不命中**（「未知≠否」——macd 样本不足的股不能命中「非金叉」）；candFactors 的 0=缺席语义在宽表升级为显式 NaN，新增因子别拿 0 当缺失。③涨停判定：阈值=limitUpPctFor−0.2（主板 9.8/创业科创 19.8/ST 4.8），**收盘涨幅口径**（相邻 close 比），ST 判定依赖 states.name——宇宙字典 name 空时按非 ST 处理。④新鲜度：表 TradeDate=**states MAX(last_bar_date)**（5500 行小表；别改成扫 daily_bars MAX——130 万行对该查询无友好索引），60s 缓存；个股末根≠表日期=stale，**扫描默认排除**（停牌股旧价因子会误导），include_stale 显式放开；构建的 DB 读 `ORDER BY symbol, trade_date` 恰与唯一索引序一致（免 filesort），别改列序。⑤构建互斥：ensureFactorTable（同步构建+双检）与 RebuildFactorTableAsync（TryLock 防抖）**共用 factorBuildMu**——重建进行中扫描请求阻塞等新表而非双建；重建挂点=16:10 增量成功后+初始化轮 Succeeded>0 后，删挂点会让宽表永远陈旧只剩懒加载兜底。⑥内置策略（`screener_builtin.go`）只能引用 factorDefs 内因子（TestValidateCondTree 全量校验 21 个内置树）；**宽表没有估值因子**（PE/PB 是实时单只接口无法全市场普查）——破净类策略别写进内置；DSL 的 Value 用 *float64 区分「显式 0」与「未填」，between 上下界宽容交换，all/any 同节点互斥。⑦推荐池 strategy_signal 来源 **best-effort**：宽表未就绪/无日线时 strategySignalHits 返回 nil 静默跳过（与单榜失败降级同款，别改成报错阻断建池）；命中进池后**批量拉实时行情覆盖收盘口径 Price**（盘中生成推荐防滞后一天）；来源字典 sourceLabelCN（后端）与 Recommendations.vue SOURCE_LABEL（前端）两处必须同步。
- **扩展数据接入（M3a）防回归**（2026-07-09）：①龙虎榜两报表必须走 `emlhb.go`→DataCenterQuery 网关（包级令牌桶）；字段实测锚点：上榜原因在 **EXPLANATION**（EXPLAIN 是「N家机构买入，成功率x%」附注可 null）、`SECURITY_TYPE_CODE` 058 前缀=股票（060=可转债必须过滤——转债也是 6 位数字代码，isSixDigits 挡不住）、同股同日多原因行以 **CHANGE_TYPE** 为唯一键第四列（别改成 reason 文本）；机构统计表名 `RPT_ORGANIZATION_TRADE_DETAILS`（FREECAP 单位是亿与主表元不同，故不落库）。②涨停池族（`eastmoney_ztpool.go`）：**sort 参数必带**（缺失 rc=102 data=null——涨停 fbt:asc/昨日涨停 zs:desc/强势 zdp:desc）；价格字段 ×1000；**上游不可回溯：date 传旧日期会静默返回最近交易日数据，qdate 校验不符必须拒绝**（2026-07-09 LIVE 实测请求 0708 返回 0709 被正确拦截）——情绪序列靠每日盘后快照积累，错过采集窗口=诚实缺失该日，别试图补历史；明细先删后插（盘中重跑池成员会变）。③人气榜 hisRc 实测负值是 **-3 而非传闻的 -1**，判新上榜统一用 `<=0` 别硬编码具体负值；sc 前缀 SH/SZ（BJ 排除）。④资金流：排行走 clist 换 fid（f62/f164/f174 白名单），主域限流时断路器自动降级 push2delay（实测备用域可用）；单股历史 fflow/daykline 15 列含收盘/涨跌幅（免二次对齐日线）；**persistFundFlow 16:00 前必须丢弃「今天」的行**——盘中 main_net 是半截值，若落库且新鲜判定认为已有今日数据将永久残留（与除权检测排除今天的根同理）；新鲜判定=末行≥上一开市日；茅台实测上游只回 120 根（lmt=250 非保证值，消费方别假设满窗）。⑤评分融合纪律：computeScore 纯函数**不动**，主力资金分经 `applyFlowScore` 外层包装融合量能维（0.6 原+0.4 资金分，样本<3 日原样返回）——既有对拍/单测口径不受影响，改权重两处常量（flowVolumeWeight）一起看。⑥值域同步铁律（第三次重申）：candidate 的 LhbNetYi/OrgNetYi/PopRank/PopPrev/OrgBuys 与 factors 的 MainNet5dYi/MainNetDays 已进 candidateValueSet；日后加信号字段必须同步。⑦龙虎榜/人气/资金流因子全是 **T-1 信号**（最近有数据交易日口径）——盘中生成推荐时今天的榜未出，lhbSignalsFor/popSignalsFor 按 MAX(trade_date) 查询是有意设计，别改成恒查今天（会全空）。⑧盘后错峰：16:35 涨停池+人气（避开 16:10 日线）、18:45 龙虎榜（避开 19:05 财报；datacenter 数据 18 点后基本齐全，ErrNoData=未发布下次再试游标不推进）；人气榜是实时榜，仅目标日=今天才采。
- **5 分钟线与盘中因子（M3b）防回归**（2026-07-09）：①腾讯 mkline（`datasource/tencent_mkline.go`）m5 行 8 列**列序是 [时间,开,收,高,低,量,{},分钟换手]**（腾讯 kline 族「开收高低」惯例，别按东财「开收高低」以外的直觉改）；量单位手；**无成交额列**——VWAP 由 量×典型价((O+H+L+C)/4) 估算（与 chip.go 筹码峰同口径，改一处两处一起看）；非法代码返回 qt 空数组且**无 m5 键**（据此判 ErrNoData）；count 上限实测 800（≈18 交易日），盘中请求末根是进行中的半截 bar。②IntradayFactorDaily **只存物化因子不存原始 bar**（一行一股一天；原始 bar 是日线体积的 48 倍，个人库不划算）——日后要新盘中因子只能从当日起积累，不追溯（上游深度 18 日，与涨停池「靠快照积累」同一诚实原则）；错过采集窗口按游标补跑（上限 intradayBackfillMaxDays=10 日），更早诚实缺失。③computeIntradayFactors 锚点根（0935/1030/1430/1500）**按时间戳后 4 位查找而非下标**（缺根时下标错位会把 11:00 当尾盘）；<46 根整行不落（宁缺勿错——盘中拉取/半日停牌的残缺日不硬算）；半场零成交拒算。④同步 job：**先删目标日再插**（事务）幂等；连续 20 只网络类失败判腾讯限流整轮中止**且不落库**（半截快照落库会让「先删后插」删掉已有完整数据换半截的）；cutoff 15:10（收盘即终态，启动补跑及时）/常态定时 17:05（错峰 16:10 日线/16:35 涨停池/18:45 龙虎榜/19:05 财报——腾讯域独立，错的是进程内高峰）。⑤盘中因子是 **T-1 信号**（intradaySignalsFor 按 MAX(trade_date) 查询，盘中生成推荐时今日 job 未跑），加分只在 IntradayDate 非空时动分且**短线专属**（长线不吃日内噪声）；candFactors 数值四件（tail30_chg/tail30_vol_pct/morning_chg/close_vs_vwap）已进 candidateValueSet（值域同步铁律第四次重申）；本批版本 p10/s8。⑥**盘中策略回测满 60 交易日才开放**（intradayBacktestMinDays，诚实门槛）：选股宽表与回测因子体系**有意未接入**盘中因子——接入前先查 IntradayAccumulatedDays()≥60，且宽表接入会破坏「因子=日线派生」的单一数据依赖假设，需重新评估新鲜度判定。
- **回测时光机与回溯诊断（M2）防回归**（2026-07-08）：①**无未来泄露是生命线**：回测信号日因子=对「截至该日的日线切片」复跑 `computeWideRowOpts`（`factortable.go`），与宽表/选股页同一套函数——**严禁另写一套「回测版因子」**（口径分叉=对拍失效=回测结果不可信）；`TestBacktestNoFutureLeak` 锁切片求值与独立数组求值逐列相等；`computeWideRowOpts(withChip=false)` 只是跳过筹码复算（回测热路径提速），非筹码列与全量必须逐列相等（`TestComputeWideRowOptsParity` 锁定），树引用 chip_* 时（treeUsesChipFactor 含 Ref 检查）必须传 true。②条件求值复用：回测 worker 用**单行 FactorTable（newRowTable/fillRow0 就地覆写）走 evalCondRow**，别为性能改写独立求值器（NaN 恒不命中等语义会漂移）。③**A 股约束五件套一条不能少**（`simulateHold` 纯函数）：次日开盘成交/开盘涨幅≥limitUpPctFor−0.5 一字板跳过（开过板的机会保守放弃是刻意的）/一字跌停（high==low 且收盘跌幅达阈值−0.5）顺延卖出、顺延到末根强平必须标 forced 不许静默闭合/整百股取整不足一手 skip_cash/费率走 `tradeFee`（与模拟盘同源，改费率两处一起动）；卖出目标按**市场轴到期日**（sellDate 参数，2026-07-17 起：个股中途停牌不拉长持有跨度，到期日停牌顺延复牌卖出；空 sellDate 回退按个股 K 线数推进），nextDate 校验防「停牌期用复牌远期价格假装成交」。④市场日轴=上证基准 bars 日期序列（指数不停牌，日历+基准价一石二鸟），基准缺失回退 trading_calendar 且 alpha 如实缺席（notes 声明），别把 alpha 缺失静默算成 0；信号日采样右边界收缩 maxHold（2026-07-17 修正 off-by-one：恰好能走完最长持有期的最后一个信号日也参与），个股停牌导致的 pending 是诚实计数不计入统计。⑤**复权自洽校验**（adjustSuspect）：相邻收盘涨跌>limitUpPctFor×1.5 判断层整股剔除+结果透明计数；**跳过头 5 根**（btAdjustSanityHeadSkip，注册制新股上市初期无涨跌幅限制会误伤）；ST 摘帽/戴帽日会被误判剔除是承认的保守简化（宁可少算不可算错）。⑥回测互斥 backtestInflight 全局一个（数秒级重活），别改成按用户——DB 流式读+全市场逐日因子是共享资源。⑦回溯诊断（`analysis_asof.go`）：as_of 快照**只含日线衍生数据**（quote 由末根 bar 合成、technicals/quant_score 对截断序列复用同函数），估值/新闻/公告/财务/风险闸门一律 unavailable_note 声明缺失——**别给 as_of 模式接实时接口补数据**（那是未来数据，泄露）；`TestAsOfSnapshotNoFutureLeak` 锁「改未来数据快照逐字节不变」；prompt 回溯声明段（严禁训练记忆中该日期之后的信息/事后视角）随 p10 版本，改措辞递增版本号；AnalysisRecord.AsOf 列已进 History 显式选列清单（新列必须同步加 Select 否则列表恒空）。⑧hindsight 端点对普通个股分析（按创建日）与回溯分析（按 as_of）都可用，评级命中以 +20 交易日收益方向为准（neutral 不判）；目标价/止损价是**调用方可选参数**（分析结果无固定价位字段），别从分析文本里「智能提取」价位（幻觉放大器）。

- **交易员阶段/提示词扩展/板块热力图（M3c）防回归**（2026-07-09）：①交易员阶段（`service/analysis_trader.go`）：仅个股+标准模式+非 as_of（回溯的历史现价上做「当下计划」无意义且易误读）；评级偏空/风险闸门 block/现价缺失三种**确定性拒绝不发起 LLM 调用**（零 token）；`validateTradePlan` 硬纪律（止损<现价、止损<买入区间下沿、目标>区间上沿与现价）校验失败走 callWithRepair repair，始终不过=不带 trade_plan（best-effort 不影响主分析）；盈亏比 =(目标−区间中值)/(区间中值−止损)，<2:1 服务端减半仓位并记 discipline_notes——**别把降仓逻辑挪进 LLM prompt**（自洽纪律必须程序保证）。②仓位公式纯 Go（computePositionAdvice）：vol20=近 20 个日收益率样本标准差（%），仓位=100×clip(2.5/vol20,0.3,1.0)×clip(0.6+(涨家占比−0.5)×1.2,0.3,1.2)；**breadth 缺失择时按 0.6 中性并声明**（别改成 1.0——比涨跌均衡的市场还激进）；<21 根收盘价仓位诚实缺席；breadth 并发预取（goroutine+缓冲 1 通道）别改回串行。③信任层三件：模型自附的 trade_plan/rr_ratio/position/discipline_notes 解析前剥除（parseTradePlan/parseAnalysisResult 两处）；计划价与公式数字**并入核验值域**（模型自身结论是合法来源，同推荐计划价前例）；**reviewAnalysis 喂复核员的 result 必须剥掉 TradePlan**（计划价不在快照中，复核员按「数字须与快照一致」审会误判捏造触发错误 reject）。④提示词扩展：module 合法性=promptModuleLabels 键集单一权威（validPromptModule 函数），加新模块只动 labels/Modules order/placeholders 三处；**占位符宽容渲染**（renderPromptTemplate：{{name}} 已知替换、未知/拼错原样保留，别改成报错）；版本归因统一走 promptVersionFor（-custom 后缀），分析记录在 module 或（Verify 时）review 模板任一启用即标 -custom；qa 会话 prompt_version 创建时固化+每次 ask 随 finalize 刷新（模板中途启停记录跟随实际用量）；**recommend/qa 模板只替换角色段**（schema 段 shortTermSpec/快照注入不变），**daily/review 模板是整段系统提示含 JSON schema**——UI 已提示用户保留 schema 段，改这两个默认 prompt 时递增 d1/复核无独立版本但分析记录带 -custom；反编造靠 parseAndFilterPicks 程序兜底，自定义文本弱化纪律放不进池外标的。⑤板块热力图（`datasource/eastmoney_board.go`）：**实测锚点**——行业 fs=m:90+t:2/概念 t:3 各约 496 个（total 字段两类都回 496 不可用作完整性断言），fid=f6 成交额降序 Top100 单页（pz 上游硬钳 100）；字段 f12/f14/f3/f6/f104 涨家/f105 跌家/f128+f140 领涨股；成分股 fs=b:BKxxxx（f2 现价/f8 换手/f20/f21 市值）；板块指数日线 secid=90.BKxxxx 与个股 kline 同 URL 同解析（parseEMKlines 抽公用，**别复制粘贴**），BK 码 ^BK\d{4}$ 校验后才进 URL；全走 e.get push2 断路器（主域限流 push2delay 实测可用；kline 走 push2his 无备用域，本机限流属常态，LIVE_BOARD=1 冒烟留部署环境）。⑥BoardService 60s per-kind 缓存+fetchMu 简版 singleflight（并发首访只一个打上游）；markBoardStocks 龙头须成交额>0、领涨须涨幅>0（上游 "-" 容错为 0 的退化行不硬标）；本批版本 **分析 p12、日报 d1（新落库）**。
- **机构观点接入（P3a）防回归**（2026-07-10）：①研报走 `datasource/emorgview.go` 的 GetStockReports——reportapi.eastmoney.com 独立域名**不走 datacenter 网关也不走 push2 断路器**，自建包级 repThrottle 500ms（对齐 QPS≤2 纪律，repBaseURL/repMinInterval 变量供单测替换）；机构调研 GetOrgSurveys 走 DataCenterQuery 网关（包级令牌桶共享）。②上游字段锚点（2026-07-10 实测）：`ratingChange` 枚举 **0=上调 1=下调 2=首次覆盖 3=维持 空串=无评级（解析归一为 -1）**——判变动以它为准，`lastEmRatingName` 上调样本也常空（缺失≠首次覆盖，别拿 last 空判首次）；`indvAimPriceT/L` 目标价字符串数字覆盖率仅约 1/4（消费方必须容错，`target_price=0` 是「未给」不是 0 元）；上游**默认 publishDate 降序稳定翻页**（第二页实测无缝衔接，`rows[:orgReportKeep]` 截断保最新依赖此序，改排序参数前先实测）；调研 RPT_ORG_SURVEYNEW **一机构一行明细**（同次调研每家机构各一行），落库前必须按 (symbol, survey_date) 聚合（aggregateSurveys 纯函数），org_count=当日行数。③按需拉取缓存照 F2 模式：7 天新鲜期**复用 finFresh**（MAX(updated_at)，无 market 条件——symbol 六位数只在 cn 落库无混淆）+1h 尝试冷却包级 `orgSyncTry`（"rep:"/"svy:" 前缀，先记后拉防并发双拉），触发点只有详情页 Overview 与个股 AI 快照 orgViewBrief，**严禁全市场普查**；Overview 的 summary 必须吃全量窗口再截明细列表（截 30 份喂 computeOrgView 会把 180 天分布算残）。④computeOrgView 纯函数声明**降序输入**（loadOrgRows ORDER BY report_date DESC 保证），rating_changes 循环靠降序 break——喂乱序数据变动计数会漏；日期窗口比较是字符串字典序（YYYY-MM-DD）。⑤消费纪律：org_view 段进个股分析/问答快照（数值叶子 snapshotValueSet 自动进值域；**computeOrgView 输出的文本字段刻意不含小数数字**，新加字符串字段别塞带小数的文案，否则要照 news 前例走 decimalNumbersIn）；guidance/qa intro 声明卖方乐观偏差（评级下调/目标价偏离/调研密度才有信息量，禁以买入家数论多）；trade_plan 的对照锚**零结构改动**——快照整体 Marshal 天然含 org_view，只在 tradePlanSystem 加了对照说明；**evidenceNoiseInts 已加 180**（机构观点窗口整数，删掉会让模型复述「近 180 天」被误报幻觉；90/30 由 ≤99 规则天然豁免）。⑥as_of 回溯快照走独立 buildStockSnapshotAsOf **天然不含 org_view**（研报按当前时间拉取，进回溯快照=未来泄露）——别给回溯模式补机构观点。⑦推荐侧本批**零改动**（p10/s8 不动）；日后接长线加分必须同步 candidate 字段+candidateValueSet+版本递增三件套。本批版本：分析 **p13**、问答 **q8**。
- **板块数据增强（P3b）防回归**（2026-07-14）：①clist 估值字段（`eastmoney_clist.go`）：f9=市盈率(动)/f115=市盈率(TTM)/f23=市净率/f100=行业板块名（2026-07-10 实测锚定；**f100 与 m:90+t:2 行业板块 f14 名精确匹配 110/110**，估值聚合的名字映射依赖此不变式，上游改名靠聚合日志「未匹配被跳过」暴露）；退市壳票 f100 与估值字段全 "-"（Industry 归一空串、emNum 容错 0），**聚合侧按 >0 过滤正样本**——PE 亏损为负、0=缺失，别把 0 当「零估值」算进中位数；翻页/半截拒收/停牌容错/fid=f12 稳定序纪律全部不动。②板块资金流：GetBoardFundFlow 与个股 fflow/daykline 同接口同 15 列（Close=板块指数点位），**直接调用 parseStockFundFlow 同包复用别复制**；走 push2his 无备用域断路器纪律（本机限流常态，LIVE_BOARD 冒烟留部署）；BoardService.FundFlow **纯透传不落库**（上游自带全历史，无需积累——与涨停池「靠快照积累」相反的场景，别画蛇添足建表）+per-code 5min 缓存与 heatCache 共用 fetchMu 防击穿；板块版汇总小函数（boardMainNetSum/boardNetStreakDays）吃 []datasource.StockFundFlowBar——**fundflow.go 既有纯函数签名（[]model.FundFlowDaily）不动**，别为板块改签名。③估值聚合表 BoardValuationDaily：**MedianPETTM 必须显式 `gorm:"column:median_pe_ttm"`**（GORM 默认蛇形化成 median_pettm，与 upsert AssignmentColumns 不一致会让覆盖永远失败——F2 批 YoY→yo_y 同款坑，本批单测当场抓住）；唯一键 (kind,board_code,trade_date) upsert 幂等；**只覆盖行业板块**（个股 f100 只有行业归属，概念板块无覆盖是范围诚实不是缺陷，估值卡/AI 段对概念自然缺席）；横截面分位对称定义 100×(less+0.5×equal)/n（单样本=50 不武断），**无正 PE 样本记 -1**（区分「最便宜」与「算不出」，前端/prompt 按 -1 显示「—」）；250 日时序分位**查询侧算**（boardValuationFor）带 hist_days 积累天数，UI 与 prompt 必须声明天数（同涨停池诚实原则）。④聚合挂点=marketwide 16:10 增量 Succeeded>0 处与 RebuildFactorTableAsync 并排（AggregateBoardValuationAsync，TryLock 防抖 best-effort）；GetBoardList 半截清单整轮拒收（映射缺一半会静默丢行业）、拉取失败只日志次日再来，别改成重试轰炸。⑤板块 AI 分析两段：focus 名**精确匹配** board_name（boardValuationByName，匹配不上是常态返回 nil 两段缺席）；board_flow 只取近 10 日+汇总控快照预算；数值叶子 snapshotValueSet 自动进核验值域（无需手工登记，但**日后给视图加带小数的字符串字段要照 news 前例走 decimalNumbersIn**）；AnalysisService 注入 BoardService（api.go boardSvc 创建在 analysisSvc 之前，别挪回去造成 nil 注入）。本批版本：分析 **p14**。
- **AI 白话建策略（P3c）防回归**（2026-07-14）：①`service/screener_ai.go` 的 prompt 因子字典由 **factorDefs 程序生成**（screenerFactorDictLines：键｜中文名｜单位｜说明｜典型阈值），`TestParseStrategyPromptFactorDict` 锁每个因子 key/中文名必在 prompt——**别手抄因子清单**（新增因子自动跟随，手抄会漂移）；parseFactorHints 阈值提示 map 的 key 必须真实存在（同测锁死），改因子 key 两处一起看。②**unmatched 兜底纪律**：LLM 输出 {tree, unmatched[], explain}，无法映射的表述（估值/资金流/财务/板块题材等宽表没有的数据）如实进 unmatched，**禁止硬凑相近因子**（prompt 纪律段+few-shot 第三例锁语义）；tree 为 null 仅当 unmatched 非空时合法（parseStrategyLLMOutput 拦「空手交差」）。③树校验全走既有 validateCondTree（因子白名单/深 6/叶 48）+describeCondTree 人话回显，校验失败 repair 一次（parseStrategyRepairMax=1），仍不过**报错不出降级半成品**（交互式动作，脏树落编辑器危害大于重试成本）。④配额：一次解析=1 次手动动作（consumeQuota manualAction=true 只调一次，repair 不重复计次；网络失败但已耗 token 也照记账计次）；LLM 走 ResolveForUse（无配置回退管理员链路自然生效），allowPrivate 按配置所有者放行（llmAllowPrivate）。⑤**AI 只生成不执行**：ParseStrategy 不落库不扫描，树由用户在前端确认（「套用到编辑器」）后走既有保存/扫描链路；前端嵌套 any 树走只读条件清单形态（aiAdvancedTree，行式编辑器仅支持一层 all 是既有约束），saveEditor/tryScanEditor 统一走 editorTree()（AI 嵌套树优先）——改编辑器保存逻辑别绕过它。⑥解析 prompt 版本 **sp1**（screenerParsePromptVersion，独立于分析 p*/推荐 s* 序列，随响应返回供归因；不落库——自定义策略表无 LLM 字段，树本身与来源无关）；改 prompt 措辞/示例/纪律段递增 sp 版本。
- **AI 请求超时与生成链路（异步任务化批）防回归**（2026-07-14，架构说明见 ARCHITECTURE §6.10）：①**生成接口 processing 语义**：推荐 `Generate`/日报 `GenerateFor(manual)` 同步段只做校验/配置解析/配额熔断，落 processing 行立即返回——**别把建池/快照/LLM 挪回同步段**（那正是 60s 超时的根源）；后台用 `context.Background()`+deadline（recJobTimeout=6min/reportJobTimeout=8min），**严禁复用 `c.Request.Context()`**（浏览器断开会取消任务）。`GenerateAuto`（日报调推荐）保持同步执行但同样先落 processing 行——**手动与自动完全同链路**不变式仍在。②**批次/报告回写用 Save 不是 Create**（processing 行已存在，Create 会造出第二行）；失败必须走 failBatch/状态回写，别裸 return 留永久 processing。幂等：15min 内 processing 直接复用（防重复点击双任务双烧 token）、超 15min 惰性判 failed（进程重启遗留）——`reuseProcessingBatch` 的 stale 清理别删。③**日报重生成保旧**：旧行原地置 processing、内容字段全程不动，双败回滚 `plan.oldStatus`（**用 Updates 精确写 status/error，不能 Save 整个对象**——内存里 SnapshotJSON 等字段已脏）；**别改回「先删旧报告再生成」**。卖点提醒改为新推荐就绪后先删当日旧规则再建新（推荐失败时旧规则保留）。④**量化降级边界**：只对 `quantFallbackEligible`（超时/网络/5xx/流中断/空内容）触发 `buildQuantFallbackPicks`；**「宁缺毋滥拒选」（picks 显式空数组）语义不动**——模型明确不选时禁止量化票硬塞；鉴权/404/Base URL/配额类确定性错误直接 failed 让用户修配置。降级条目恒 watch+置信 35+`degraded_source=quant_fallback`+SysConfidence 强制 low（核验全绿≠结论可信）；ATR 规则价位（止损 2×ATR%/止盈 3×ATR%/区间 ±0.5×ATR%，ATR% 钳 [2.5,6]）天然满足 shortPlanPricesValid 四价关系——改系数前先过 TestQuantFallbackPicks 手工验算。⑤**输出预算**：`capModuleTokens(用户 max_tokens, 模块上限)` 三处上限 复盘 1500/推荐主调 2500/复核 1500——压得更低会截断 JSON 反而触发 repair 更慢；recRepairAttempts=1、坏输出回灌截断（推荐 600/日报 800 字）；prompt 输出纪律段改动须递增版本（本批 **推荐 p11、日报 d2**）。⑥**审计实际形态**：chatCompletion 回落非流式时审计记 stream=false（`streamed` 指针传递），`first_chunk_ms` 只在流式路径>0、整包兼容路径≈latency_ms（识别假流式网关）——**responses 分支别再自埋点**（同两出口覆盖不变式）。⑦追踪：refreshBatches 条目并发 4+upsert 收集后串行（SQLite 不吃写并发）；**degraded 批次已纳入追踪**（降级推荐有规则价位；老式无条目 degraded 读空自然跳过）。⑧日报复盘与推荐**两路 goroutine 并行**（TestDailyReportAsyncParallel 用双向进场信号锁死，改回串行会挂）；DailyReportService 的 snapshotFn/recFn 是测试注入点（nil=默认实现），别删。⑨前端：生成请求已撤 AI_TIMEOUT（接口秒回），轮询走 `lib/poll.ts` 的 pollUntil（2.5s 间隔/8min 上限）；两页 onMounted 对 processing 记录恢复跟踪——改列表接口时 processing 行必须仍出现在列表里。
- **体验杂项批防回归**（2026-07-14）：①推荐复盘已读 `RecommendationStatus.ReviewAck` 是人工标记，**绝不能加进 tracking.go upsertStatus 的 AssignmentColumns**——每 2h 追踪刷新 upsert 会把已读打回未读（列刻意不在覆盖清单里，tracking.go 有注释）；今日待办 rec_review 条目 `RefID` 是追踪状态行 id（不是 BatchID），已读消项按它调 `PUT /api/recommendations/review-ack/:id`，跳转批次另有 batch_id 字段。②日报删除 `DELETE /api/daily-reports/:id`：**processing 且创建 <15min 的拒删**——后台 goroutine 仍会回写 Save，先删后回写等于「删除名存实亡」（行复活）；超 15min 的 processing 是死任务可删。删除**不级联**关联推荐批次与卖点提醒（独立生命周期）；今日日报删后仍在自动生成窗口（15:35~20:00）内会被 job 重建，前端删除确认已提示。③排除创业板/科创板 `RecFilters.ExcludeGemStar`（json `exclude_gem_star`）按 30/68 前缀在阶段②透明排除并 Describe 回显；存 RecFiltersJSON 无需迁移，默认 false；日报自动推荐同筛选生效。④AI 产出 LLM 标签：DailyReport 落库 `llm_config_id/provider/model` 三列（生成时写入，List Select 已加列）；CompareResult/ParseStrategyResult 同款字段**不落库随响应回传**；前端 `composables/useLlmLabel.ts` 模块级缓存 listLLMConfigs 的 id→配置名映射——配置已删/管理员回退配置显示回退到 provider，旧记录空串靠 v-if 兜底，新增 AI 展示处照它接线别自建映射。
- **推荐准确性第一批（S0 测准 + S1 止血，2026-07-16）防回归**（规划全文 `docs/RECOMMENDATION_ACCURACY_PLAN.md`，改标签/门控/仓位/归因前先读）：①**统一执行模拟器 `execution_sim.go` 是执行语义单一权威**（simEntry/simulateHold/simulateLabelHold/isOneWordLimitDown/adjustSuspect/cnDailyBarsAsc 从 backtest.go 抽出，五件套语义与既有回测测试逐项锁定）——tracking 标签结算、BatchBacktest、条件树回测共用，**严禁再写第二份执行逻辑**；simulateLabelHold 的障碍语义：T+1 买入日不判止盈止损、同日双触保守取止损、成交按障碍价（不取更优 high/low）、止损日一字跌停顺延收盘卖出。②**标签事实两类分表铁律**（`recommendation_labels`）：entry_mode=next_open 是模型结果事实（统一模拟，供训练评估）；entry_mode=actual_position 是用户执行事实（持仓血缘实际买入价，**禁作训练标签**，结算不判障碍）；影子标签=RecommendationID=0 且 CandidateEventID>0（被过滤/被门控/落选标的的对照收益）；唯一键 (rec_id, event_id, horizon, entry_mode)；成熟推进挂 StartTrackingJobs 每 2h（AdvanceRecommendationLabels 幂等，matured 不重算；单轮上限 labelAdvanceBatch=4000）。③**S1-1 regime 影子模式三不**：不注入 prompt（会让 LLM 自行少发 buy，污染影子对照——BenchTrend 既有弱势提示保持不动别扩展）、不改写 action（`recRegimeEnforce` 包级 flag 默认 false，**转正必须凭影子配对数据评审**，验收协议见规划 §5 S1-1）、不跳过落库（拒选/降级批次也落 Regime/RegimeJSON）；判定参数 defaultRegimeParams 随 RegimeJSON 快照落库可回溯，改参数/信号递增 regimeVersion（rg1）。④**S1-2 仓位公式单位口径**：vol20_annual = candFactors.Volatility20（日收益标准差 **%**）/100×√252（小数），公式 min(波动分档上限, target/vol×100)×regime系数(1.0/0.6/0.3)×相关性系数，整批 Σ 超预算（100/60/30%）按比例缩；参数 defaultSizingParams（ps1）随 RegimeJSON 落库；Volatility20<=0 时仓位如实缺席（PositionWhy 说明），别拍默认值。⑤**S0-4 四修**：买入胜率只统计 buy AND 成熟（短线终态/长线超 60 交易日，statusMatured；watch 单独、active 计数不进分母、degraded 批次条目整体剔除 DegradedExcluded）；追踪并列记 actual_buy_price/actual_return_pct（在 upsert 覆盖列里，属程序算的可覆盖，与 review_ack 人工标记不同）；**防前复权重锚**——Recommendation.RefDate/RefClose 存生成时收盘版本（scorePool 的 lastBar* 工作字段带出），tracking/标签结算比对同日收盘偏差>rebaseTolerance 即按复权因子调整 RefPrice/止盈/止损再评（note 声明「已按复权因子调整」）；**自选 Amount=0 不再绕过流动性门槛**（buildPool 先按近 20 日日线中位数 medianAmountsFor 补齐，补不到=拒绝，candidateEligible 的 `c.Amount > 0 &&` 旧短路已删，改回会复活 bug）。⑥**S1-3 去相关在名单阶段生效**（scorePool 尾部贪心：相关性 ≥0.85 只留分高者、同行业 ≤2 只），**不改写 LLM 输出**——被挤出者保留池内排名、事件表记 gate_type=correlation/industry_cap；行业来自宇宙快照 industriesFor（未积累时行业约束自动缺席是常态非 bug）；candidate 的 closes/lastBarDate/lastBarClose 是小写工作字段不进池快照 JSON。⑦**S0-5 反事实事件**：recordBatchFacts 在批次落库后 best-effort 全池落 candidate_events（picked/llm_list/filtered/pool_full/scored 五 stage）+每候选 5 horizon 标签；影子期 would_be_action 非空但 post_gate_action 恒=raw action。⑧**S0-3 宇宙快照**：SyncMarketWide 顺手 upsertUniverseDaily（同一次 clist 请求零上游成本，唯一键 trade_date+symbol），5500 行/日是有意设计别加采样；历史回放候选宇宙按它重建防幸存者偏差。⑨S0-6 归因报表（RecAttribution）只吃 rec_id>0 + next_open + matured 样本——影子与 actual 不混入；本批版本：strategy **s9**（名单去相关）、label **l1**、regime **rg1**、sizing **ps1**。
- **推荐准确性第二批（S2 影子件，2026-07-16）防回归**（规划 §5 S2；LLM 件全部先影子，改反方/质量门控/影子报表前先读）：①**影子三不纪律延续**：反方研究员（bear）与数据质量门控只展示/只记录，**不改写 action/置信度**——applyBearShadow/applyQualityGateShadow 是纯回填函数，程序裁决（high→watch+置信≤40、质量封顶）的转正必须凭影子对照报表 gated vs ungated 配对评审，评价协议预注册（规划 §5 S5），不允许看表面胜率拍脑袋启用。②**反方研究员（recbear.go，br1）**：只对 action=buy 条目调用（无 buy 零调用零成本）、放在 verify 复核**之后**（被否决降 watch 的条目不再消耗反方篇幅）；调用预算=主调 1+复核 1+反方 1=**上限 3 次不变**；BearCheck 开关 nil 时**默认关联 verify**（prepareGeneration 里 3 行，前端 watch 联动同款语义）；bear prompt 注入 A 股 bear 论据框架（解禁/T+1 锁仓/高位放量/拥挤/估值）但**解禁/减持数据本系统未提供——只能提示核查，严禁虚构**（TestBearPromptFramework 锁关键词）；severity 归一 high/med/low（normalizeBearSeverity），只有 **high 且 buy** 记 gate_type=bear_shadow 事件（would_be_action=watch），影子收益由标签任务自然结算。③**质量门控（recquality.go，qg2）按策略分场景**：长线缺 fin=严重（cap 40）、短线缺 fin 不标记；行情/日线过期（交易日历口径：错过 >3 个开市日；无日历回退自然日 >5——2026-07-17 qg1→qg2，自然日口径在春节/国庆长假会误判过期）/样本不足（<60 根）/成交额缺失属通用严重；**情绪缺失≠情绪中性**——SentiNews==0 是数据缺失（senti_missing 独立标记，不降 cap、不进 missing_critical_fields），SentiScore=0 且有新闻才是中性；多项命中取最小 cap；**只在 would-be cap < 该条最终置信度时记 quality_shadow 事件**（cap 不构成约束的只落 pick 明细）；改判定规则/阈值必须递增 qualityGateVersion。注意这是本项目**原创硬门**（astock quality_gate 只注入不硬禁）。④**服务端字段防伪造**：normalizePick 入口剥除模型自附的 Review/Bear/QualityGate（verify/bear 关闭时无人覆盖，假面会落库展示）；**DegradedSource 不剥**（quant_fallback 构造路径先设值再过 normalizePick）。⑤**事件表每候选恰一行**是影子标签唯一键的前提（picked 条目两行事件会生成重复 (rec_id,0,horizon,mode) 标签撞唯一索引整批失败）——同一标的多门控（regime+bear+quality 可三重叠加于 picked buy）走 **mergeGateNotes** 合并：主 gate_type 按优先级 regime>bear>quality>correlation>industry_cap，次要门控说明并入 reason、**全部类型记入事件行 GateTypes（逗号分隔，2026-07-17 加列——只留主类型会让次要门控永久丢失样本）**；gateNote.GateVersion 空=沿用 regimeVersion（第一批落库口径兼容）。⑥**影子对照报表（recshadow.go）**：对照组=「未被**任何**门控标记的入选成熟 **buy**」（2026-07-17 起 gated 与对照统一限定 buy——watch 本就是更弱信号，混入会让转正判断失真；correlation/industry_cap 标记的落选票吃影子标签不受 buy 过滤）；按 GateTypes 拆分归组（旧行回退单值 GateType），样本可在多组重复出现组间不可加总；类型过滤靠 labels（events 无 type 列，Marked 按事件∩标签口径计）；报表统一吃 next_open 标签可比；覆盖率 would_rewrite=若强制损失的 buy 数（**只计会改写动作的 regime/bear**——多门控事件携带的 would_be_action 属于主门控，不得记到 quality 组头上）。⑦**S2-1 反思表只建了结构**（recommendation_reflections，唯一键 rec_id+horizon）：LLM 反思生成与注入**延后至成熟标签 ≥30 条**（规划 §5 S2 排序原则），available_from 字段防记忆回放泄漏——启用时只能注入 available_from ≤ 生成时点的教训。⑧本批不动主 prompt/策略版本（bear 是独立调用、质量是纯程序、regime 仍不注入）：推荐 p11/s9 不变；新版本号 bear **br1**、quality **qg1**。

- **推荐准确性第三批（S3 前半：因子快照 + 召回评估 + IC 验证，2026-07-16）防回归**（规划 §5 S3-1/S3-2/S3-4；本批纯测量零门控零 LLM 调用，改快照/召回/IC 前先读）：①**S3-1 因子快照不可变纪律（factor_snapshot_dailies）**：已落库的 (trade_date, symbol) 行**不可覆盖**（2026-07-17 起同日只**补缺失 symbol**——分批历史初始化首批先落一部分，整日跳过会让补齐股票永久缺席）——daily_bars 是前复权序列会被除权重锚整股重写，覆盖会把「重写后」的值伪装成当时快照，正是本表要防的 PIT 泄漏；挂点在 RebuildFactorTableAsync 成功发布新表之后（16:10 增量/管理端手动/历史初始化推进都走它），ensureFactorTable 懒加载路径**有意不挂**；存储=一股一日一行 JSON（NaN 因子不落键，~4MB/日），列式 52 物理列会锁死因子演进别改回去；stale 行（LastBarDate<TradeDate 停牌股）也落库不排除——停牌本身是当日事实，消费端自行过滤；版本 **fv1**（factorDefs 清单/口径变更时递增）。②**S3-2 召回评估（recrecall.go）机会集口径**：宇宙快照（!ST/!停牌/成交额 ≥3000 万）∩ simEntry 可成交（一字板买不进/次日停牌剔除）——**买不进的标的不许进机会集分母**；收益一律 simulateHold 统一执行净收益（与标签 next_open 口径可比，别换成裸涨跌幅）；无宇宙快照的旧日期回退日线口径并在 Notes 声明幸存者偏差；来源消融按**首来源**归属（多源重叠归首来源，消融值是保守下界，诚实声明）；Top-K 并列按代码稳定排序保证可复现；持有期未走完的近期批次自动跳过（snapDate 返回空）不算失败。③**S3-4 IC 白名单纪律（factoric.go icFactorKeys）**：只收横截面可比的无量纲 A 类因子——close/ma5/boll_up 等价格量纲、macd_dif/dea/hist（随股价缩放）**排除是有意设计**；chip_* 属 A 类但每横截面全市场重算 150 价格档代价过大暂不入；历史横截面=computeWideRowOpts as-of 切片复跑（M2 同款，无未来泄露），收益=收盘→收盘不扣费（测区分度非可执行收益，净收益验证归 S3-5 walk-forward）；剔 ST 与复权断层股与推荐宇宙一致；单横截面有效样本 <50 不计当日 IC；**不设「必须删几个因子」的验收指标**，权重调整只以样本外结果为准（规划 §5 S3-4 明文）。④Spearman 用并列平均秩+对秩 Pearson（含并列的通用口径，测试锁手工验算 0.316228）；IC 报表进程内缓存（icReportCur），管理端 refresh=1 才重算，全局互斥（icInflight/recallInflight 与 backtestInflight 同款）。⑤streamCNDailyBars 是全市场日线流式单遍扫描的公共封装（召回评估在用）——轻计算消费方复用它，重计算（因子重算）仍照 backtest.go 的 worker 池模式。⑥本批不动任何推荐链路行为：p11/s9/rg1/ps1/br1/qg1/l1 全不变，无新 prompt。

- **推荐准确性第四批（S3 后半：walk-forward 评估基线 + 月度走查，2026-07-16）防回归**（规划 §5 S3-5/S3-3；本批纯测量零门控零 LLM 调用，改 walkforward.go 前先读）：①**baseline 评分复刻是口径生命线（wfScoreStock）**：五维分吃尾 factorBarLimit=90 根、candFactors 吃尾 chipBarLimit=210 根、筹码本地复算、strategyAdjust、低位高换手分级扣分（20~25% -5 / 25~30% -8）——与 scorePool 组装逐项一致，`TestWFScoreStockParity` 手工侧独立复刻组装对拍锁死；**改 scorePool 评分组装（窗口/顺序/扣分）必须同步改 wfScoreStock 并过对拍测试**，否则 baseline 评的不是线上评分。②**复刻边界（有意为之别「补全」）**：系统级排除只收换手三件（>30% 硬拦/20~30% 高位死亡换手/流动性中位数 <3000 万）+ ST + 复权断层；用户个性化筛选（股价/市值/追高保护/ExcludeGemStar/涨停排除）**不进 baseline**——评估对象是评分排序质量非个人筛选；C 类数据（估值/情绪/龙虎榜/人气/资金流/盘中/财务）无法可靠回填，相关加分历史缺席（长线策略在报表中退化为纯技术面），Notes 已声明——**别试图拿「当前最新接口」填历史日期**（PIT 泄漏，规划 §5 S3-1 明文禁止）。③**切分语义（wfSplitFolds/wfAdaptSpec 纯函数，小样本手工验算锁定）**：Purge=该段最大持有期、Embargo=5 交易日，段间 gap=purge+embargo；折**右对齐**（最后一折测试段贴数据末端，最新数据必被测试）、wfMaxFolds=3 截旧折控耗时；窗口自适应=目标 480/90/60 按 480:90:60 比例缩、下限 60/15/15、超预算从训练段扣（评估段优先保住）、放不下 0 折是合法输出（SpecNote 声明）——**手工评分无参数可拟合，训练/验证段对 baseline 是占位**，S4 模型换入时训练段拟合/验证段调参/测试段只用于最终验收的纪律从这里继承。④**评估指标口径（S3-3）**：收益=simulateHold 统一执行净收益（与标签 next_open 可比）、alpha=净收益−上证 close→close（BatchBacktest 同款）；Precision_net@K 与 Precision_alpha@K 分开、净收益中位数与严重亏损率（net<-5%）并列、短线（5/10 日）/长线（20 日）分开——长线 60 日在 250 根历史下连一折切不出，**等数据积累自动可用而非降门槛硬凑**；Top-K 并列按 score 降序+代码升序稳定可复现（IC/召回同款纪律）。⑤月度走查=每月首个交易日 × 每策略 Top10（代表持有期短线 10/长线 20），与折共用同一遍全市场扫描的观测；轴起点所在的截断月丢弃（wfMonthlyIdxs）；近月持有期未走完如实标 pending 不算失败。⑥计算路径：每信号日一次全市场 as-of 因子重算（**含筹码**，忠实 baseline，与 IC 跳筹码不同是有意差异——IC 是单因子区分度、这里要复刻完整评分），worker 池照 backtest.go 模式、扫描复用 streamCNDailyBars；wfInflight 互斥+进程内缓存 refresh=1 重算（factoric 同款）。⑦本批不动任何推荐链路行为：p11/s9/rg1/ps1/br1/qg1/l1/fv1 全不变，无新 prompt 无新表。
- **推荐准确性一致性修复批（2026-07-17，外部审查 13 缺陷 + 自查 2 个连带 bug）防回归**（改执行模拟/标签结算/评估口径前先读）：①**持有期按市场日轴推进**：simulateHold/simulateLabelHold/settleFromActualEntry 新增 sellDate 参数（市场轴到期卖出日）——个股中途停牌**不**顺延到「复牌后第 N 根 K 线」（旧格子口径会让不同股票的实际持有跨度不可比）；到期日个股停牌=想卖卖不出，顺延复牌首根收盘卖出记 Deferred；到期日在数据之外（含 labelFarFuture 哨兵）=pending。调用方（backtest 策略回测/BatchBacktest/walk-forward/召回/标签结算）全部传轴日期；**空 sellDate 回退旧格子口径**（无轴兜底），新消费方一律传轴。②**simEntry 的 pending/skip 语义**：信号根后个股暂无数据返回 **pending 而非 skip_suspend**——纯函数无法区分「停牌中」与「数据未同步到」，当晚结算的新标签必然落在这里（旧行为会把所有当晚标签误判成 skip_suspend 终态）；复牌/次日数据落库后 bars[i+1] 与 nextDate 比对自然给出 skip_suspend 或成交；「从未入场且超 90 自然日」由 advanceOneLabel 兜底 no_data。③**标签结算走 marketAxis**（AdvanceRecommendationLabels）：nextDate=signalDate 后市场首个交易日（推荐日次日停牌必须 skip_suspend，禁止用复牌远期价格假装成交——labelAxisDates 统一供标签与 BatchBacktest）；测试 fixture 若不想走市场轴口径须清空 trading_calendars（cleanLabelTables 已加）。④**障碍出场日 MFE/MAE 截断**（excursionToExit）：止盈/止损在出场日盘中离场，当日只计入开盘价与障碍成交价——出场后的同日行情不属于本笔持有（日线粒度还原不了触发前高低点，保守取已知点）；顺延/到期出场持有全日仍整根计入。⑤**walk-forward Top-K 机会集**：入场判定 skip_*（一字板/次日停牌/不足一手）的观测**先剔除再选 Top-K**（§184 口径与召回一致）——否则「推 9 只买不进+1 只盈利」显示 100% Precision；WFMetric.Skipped 保留字段恒 0。⑥**ST as-of（walk-forward+IC 共用 universeSTByDates）**：优先宇宙快照按信号日判定（后来才变 ST 的股票不得被提前从历史剔除）；快照未覆盖回退当前名称+Notes 声明偏差；「全部信号日均 ST」才整股计入 stSkipped。⑦**IC 收益窗口市场轴对齐**：未来收益终点=axis[i+h]，目标日停牌的观测记 NaN 剔除（不再顺延到复牌后第 N 根）。⑧**边界 off-by-one 三处已修**（backtest sampleSignalDates / recrecall lastEligible / walk-forward eligible）：恰好能走完持有期的最后一个信号日参与评估——相关测试断言已按新边界固化。⑨**walk-forward 折合并去重**：重叠测试段按信号日去重再合并（Fold=0 行），重复计入会虚高 Signals/Trades 且重复加权。⑩**组合相关性按交易日交集对齐**（pairwiseCorrAligned）：candidate 加 closeDates 工作字段，仓位 MaxCorr 与名单去相关两处调用已换——任一股停牌时按数组位置对齐会把不同交易日的收益配对；pairwiseCorr（位置对齐）保留给无日期序列的调用方与既有测试。⑪**归因报表 buy 口径**：action 维度并列 buy/watch 对照，**其余维度只统计 buy**（sample_buy 新字段）——watch 混入会稀释买入胜率与错误归因。⑫**GORM 蛇形化列名坑第三次出现**（PETTM→pettm，前科 YoY→yo_y×2）：upsertUniverseDaily 的 AssignmentColumns 写了 pe_ttm 导致**宇宙快照自 S0-3 上线起一行都没落过**（整条 upsert SQL 无效非单列失败）——写 AssignmentColumns 必须与 GORM 生成列名核对，TestSyncMarketWideEndToEnd 已加快照行数回归断言。⑬全部为测量/评估侧修复+1 个数据落库修复：推荐链路 LLM 行为与版本号不变（p11/s9/rg1/ps1/br1/l1/fv1）；质量门控 qg1→qg2（过期判定改交易日历口径）；事件表新增 GateTypes 列（AutoMigrate 自动加列，旧行空值回退 GateType 单值）。

- **推荐准确性外部审查修复批（2026-07-17，codex 52 项审查 8 组核验后修 ~38 项）防回归**（改执行结算/追踪/账本/数据同步前先读；纯测量与工程侧，不动 LLM prompt 文本，仅执行结算语义 l1→l2）：
  - **① horizon 持有期语义修正（l1→l2，最重要）**：持有 N 交易日=「卖出根 = 买入根 + horizon」（买入根之后第 N 个交易日收盘），**根治 horizon=1 旧口径 `buyIdx+horizon-1` 的同日买卖 T+0 违规**。改动统一 8 处出场索引：execution_sim（simulateHold/simulateLabelHold/settleFromActualEntry 空 sellDate 回退 `i+horizon`→`i+horizon+1`）、reclabel（labelAxisDates `nextIdx+horizon-1`→`nextIdx+horizon`、actualSellDate、settleFromActualEntry）、backtest（`axis[aiIdx+h+1]`、sampleSignalDates eligible `-maxHold-1` guard `+3`）、walkforward（`axis[sigIdx+hd+1]`、eligible `-maxHold-1`）、recrecall（`axis[ai+horizon+1]`、lastEligible `-(horizon+2)` guard `+3`）。**部署影响**：所有 horizon 结果整体后移一天（h≥2 相当于多持一天），故 labelVersion l1→l2；advanceOneLabel 各终态回写 LabelVersion，仍 pending 的 l1 行下轮用新逻辑结算并 stamp l2；**已终态 l1 样本不重算**、RecAttribution/recshadow 均加 `label_version=l2` 过滤隔离——归因/影子报表历史样本清零从 l2 重新积累（诚实丢弃 buggy 口径，非数据丢失）。可选人工重算 SQL：`UPDATE recommendation_labels SET maturity_status='pending', skip_reason='' WHERE label_version='l1' AND maturity_status IN('matured','no_data')`（不建议默认执行，留部署决定）。
  - **② Forced 强平持久化+分列统计**：RecommendationLabel 加 `Forced bool gorm:"column:forced"`（显式列防蛇形化坑）；顺延到末根强平/退市长停末根强平的收益偏保守（真实中卖不出），归因（ForcedExcluded）/walkforward（WFMetric.Forced）/recrecall（Forced 计数）/backtest.Run 聚合四处主统计单列剔除不进 Precision/胜率/均值。
  - **③ pending 双语义（选择效应）**：simulateHold 家族加 marketLastDate 参数——个股末根<sellDate 但市场轴已过 sellDate=退市/长停，按末根收盘强平（Forced）；市场轴也未到=真未成熟保持 pending。**根治「退市/长停坏结局高分股被静默判 pending 排除、剩余成交项 Precision 虚高」**；各调用点传轴末日。
  - **④ 已入场后长停/退市永久 pending**：advanceOneLabel btPending 分支 BuyDate 非空且超 90 自然日→末根收盘强平（Forced、matured、收益完整），不再永久滞留；与③自洽（有轴时③早强平、无轴时④的 90 日兜底）。
  - **⑤ 坏退出价守卫**：出场根 Close/exitPrice≤0→pending（与买入侧 Open≤0 判 skip_suspend 对称），不伪造 ≈−100% 成熟收益。
  - **⑥ 标签推进 keyset 分页**：AdvanceRecommendationLabels 从一次性 `LIMIT 4000` 改按 `id>lastID ORDER BY id LIMIT 4000` 循环取尽全部 pending，**根治「pending>4000 时字母序靠后的成熟短周期标签永远饿死」**；每页内仍按 symbol 分组读日线，幂等。
  - **⑦ 成熟追踪结果冻结（tracking）**：refreshBatches 对已有终态行（take_profit/stop_loss/expired）**整行跳过重算与 upsert**——根治「止盈止损后收益仍按最新价漂移、成熟胜率被后续行情改写」；**no_data 有意不冻结**（暂态非结局，冻结会让新推荐当晚落的 no_data 永久卡死，且它本被 Performance.Sample 排除）。已平仓持仓实际收益按真实 SellPrice 定格（actualReturnEndPrice）；追加实时 bar 前判交易日（shouldAppendQuoteBar，根治周末追加虚假今日 bar）；后台枚举 status IN(success,degraded)（补 degraded-only 用户）；getBench 缓存前一次性排序、benchRange 删原地 sort（消除 4 路 goroutine 共享 slice 数据竞争）。
  - **⑧ 账本完整性（recommendation.go/reclabel.go）**：零推荐批次（合法拒选）也补 recordBatchFacts 落全池 candidate_events+影子标签（可验证「拒选是否正确」，不伪造 pick 批次仍 degraded）；recordBatchFacts 改返 error+事件与标签**同事务原子落库**（杜绝半成品）；批次加 `FactsRecorded bool`，失败进程内同步重试 3 次+tracking 巡检记录 facts_recorded=false 批次（**未做跨进程持久化补建**——pool 快照缺价格锚、gates 未持久化、candidate_events 无唯一索引使影子标签重跑会重复，重建会污染影子评估）；**RawAction 用复核前快照**（rawActionBySym，复核 reject 的 buy→watch 差异得以体现，此前 RawAction 恒等于 PostGateAction）。
  - **⑨ watch 不占买入仓位预算**：applyBuyPositionSizing 只对 action=buy 条目算仓位并参与 Σ 归一化，watch 置 0——根治「观察项获非零仓位挤占真实 buy 预算」；相关性对齐也只在 buy 之间。
  - **⑩ 召回评估空池批次排除**：recrecall 批次循环 `len(pool)==0` 整批跳过（旧批次/事实落库失败批次不再被当「全量未召回」拉低 Recall），Notes 声明。
  - **⑪ backtest 条件树回测 ST as-of**：复用 universeSTByDates 按信号日 as-of 判 ST（此前只 walk-forward+IC 用了，条件树回测仍用当前名称=幸存者偏差），「所有信号日均 ST」才整股剔除、逐信号日再 as-of 排除，快照缺失回退当前名称+Notes 声明。
  - **⑫ 因子快照低质量首写**：SnapshotFactorTable 落库前只落 `init_status=done` 的 symbol——根治「首部署当天 1 根 bar 的全 NaN 快照被永久冻结」（同日补缺失+首写胜不可覆盖纪律不变）。
  - **⑬ 数据同步落库失败**：persistDailyBars 返回 error、历史初始化仅落库成功才标 done（失败保持 pending，根治永久缺口）；新闻去重登记推迟到 insertNews 成功之后、游标只推进到已入库/确认重复条目（DB 短暂失败不再永久丢新闻）；财务 optFinRefreshDay 仅 RefreshAll 无错才写、涨停池/人气榜游标拆分（optMoodPoolDay/optMoodPopDay 各自成功才推进）；新闻/公告/明日披露三处 positions 子查询加 `status='holding'`（已平仓不混入采集/名单）。
  - **⑭ 并发/安全/杂项**：日报失效任务接管改乐观锁（WHERE status+updated_at，RowsAffected==0 视为被接管）；QA 同会话进程内 per-conversation 互斥（防上下文重复/消息倒序）；提醒去重加 per-user 包级锁（手动与后台是不同实例，须包级锁）；通知换 kind 强制新 target（防旧密文被当新类型）；逻辑卡 invalidated 强制非空原因；改密三步入事务；OAuth state cookie 按生产置 Secure；multipart 前置 MaxBytesReader+MaxMultipartMemory；CSV 导出 `= + - @` 前缀 sanitize 防公式注入；持仓总览 ProfitPct 分母改「已定价成本」（行情失败仓不再稀释收益率）；待办查询失败记 SysWarn（不静默藏止损）；日报通知路由 `/daily-reports`→`/daily-report`；PositionInput.ChecklistJSON 改 *string（区分未传/清空）；同步重复触发 IsSyncingBars 预检返回 started:false；finance/mood 接口校验 market 仅 cn。
  - **⑮ 前端**：LLM 配置为空时**不再前端硬拦截**（后端 ResolveForUse 有管理员回退，llm_config_id 传空由后端兜底）；白话建策略/walk-forward/factor-ic/recall 补 AI_TIMEOUT/HEAVY_TIMEOUT（20s 默认超时会掐断合法长任务）；StockDetail/Heatmap/BoardDetail/News 加请求序号 seq 守卫（防旧响应覆盖）；loadSelf 仅 401 清票（网络/5xx 保留会话）、流式 401 与拦截器一致清票跳登录；Qa 深链改 watch(route.query)+send 会话身份守卫；poll.ts 加 AbortSignal 取消（离页停轮询）；Prompts 草稿按模块合并（不冲掉他模块未保存编辑）；ETF 自动刷新并刷 overview；首页有持仓但行情全失败显示「—」非「暂无持仓」；Alerts 0% 阈值 `??`；axios 错误提取后端 message；逻辑卡日期本地拼接（不走 toISOString 防 UTC 偏移退一天）；News 加载更多失败回滚 limit；AppShell 健康状态独立轮询；Notes 收起无条件 resetForm；AdminLlmCalls 补 screener_parse/rec_bear 模块；自动刷新排除 11:30~13:00 午休；已平仓持仓编辑只留备注；写接口返回类型改裸模型（Position/WatchlistItem 富化视图字段实为空）。
  - **明确不改（有意设计/已承认边界）**：情绪一天一算当日不刷新（批次内证据可复现，senti 因子只在 SentiNews>0 时动分）；追踪 RefPrice 双口径（展示口径 vs 标签 next_open 训练口径，note 已标注）；actual_position 标签名义一手（禁作训练标签的反事实对照）；IC 目标日停牌记 NaN 剔除（RankIC 学术惯例）；adjustSuspect 全序列断层剔除（数据质量过滤非按未来收益选样，宁可少算不可算错）；配额先查后增 TOCTOU（个人自用+限流可接受）；模拟账户 Reset 吞 JSON 错（本意就是清空）；Android allowBackup（已 skip-worktree，留部署自决）；hindsight 事后诊断按个股 bar 定位（不进训练/评估链路，低危）。

- **行情时效性 fail-closed 批（2026-07-18，codex 外部审查 P0 全修 + P1 选做）防回归**（改新鲜度判定/消费链路前先读）：
  - **① 数据源禁伪造行情时间**：东财/腾讯/新浪个股行情时间字段缺失或解析失败一律**零值**（timestamp_unknown），严禁回填 `time.Now()`——旧价打上「现在」的时间戳会被全链路当实时价。零值在 quoteFreshness 恒判 stale；stock_quotes 落库零值用 epoch 哨兵（`time.Unix(0,0)`，防 MySQL 严格模式拒绝零日期，1970 显然非真实时间不会误判 fresh）。Breadth/资金流/指数等聚合接口 DataTime 仍是采集时刻语义（遗留见下）。
  - **② quoteFreshness 时段内校验**（sessionTailSlackMin=30）：非交易时段仅「日期一致」不放行——午休要求行情时刻 ≥11:00、盘后/休市/盘前昨收要求 ≥14:30，拦「同日 09:30 停滞行情在 12:30/15:30 仍判 fresh」；宽容 30 分钟是给低流动性标的的尾盘末笔成交留余地，别收得更紧（收盘集合竞价 14:57~15:00 才产收盘价）。
  - **③ 统一新鲜度入口**（market.go）：`QuoteFreshnessOf(market, dataTime)`（任意行情的时效判定）、`FreshQuotesFor`（批量全源新鲜行情）、`FreshnessView`（API 响应 freshness 块：captured_at/source_data_time/expected_as_of/source/market_state/freshness_status/stale_reason）。**新增旧价敏感消费方一律用这些入口，别自行比对日期**。个股行情端点 GET quote 已改走 GetFreshQuote 并带 freshness 块（前端 StockDetail stale 徽标）。
  - **④ stale 禁精确交易计划**（attachTradePlan）：freshness_status 非 fresh 或带 freshness_note 一律确定性 NoPlan（零 token）——stale 行情可作「截至某时的历史解释」（正文分析仍生成），**买入区间/目标价/止损价/仓位必须建立在当前有效行情上**。注意：快照缺 freshness_status 键也拒绝（fail-closed），测试快照须显式带 `"freshness_status":"fresh"`。
  - **⑤ 推荐行情时效硬门**（applyQuoteFreshGate，qf1）：LLM 终选名单喂模型前逐只 FreshQuotesFor 核验——fresh 刷新 Price/ChangePct 并记 QuoteAsOf（随池快照与 pick 明细落库）；stale/取不到透明排除（Excluded+gate_type=quote_stale 事件，影子标签自然结算可回验误伤）；全过期宁可失败不基于旧价推荐。**这是数据有效性硬检查（同「日线拉取失败=透明排除」先例），不属于质量门控影子评审范畴**——qg2 影子纪律（预测质量门控转正须凭对照数据）不因此动摇。pick.QuoteAsOf 为服务端回填，normalizePick 剥除模型自附值。s.market==nil（单测）跳过硬门。
  - **⑥ 提醒/守护/模拟成交/追踪 fail-closed**：alert evaluateRules 与 guard 改走新鲜行情（stale 跳过该标的本轮不评，防旧价误触发）；paper 市价成交 stale 拒绝（提示手动指定价格）；tracking `shouldAppendQuoteBar` 加 dataTime 参数——行情数据时间非今天（含零值）不得追加为今日 bar（旧价 bar 会驱动止盈止损触达误判）。
  - **⑦ 日报收盘门槛**：手动生成在交易日 15:35（reportWindowStartMin，同自动窗口起点）前拒绝——早盘生成会以盘中数据冒充收盘口径并占用当天唯一记录（自动任务不再重建）。
  - **⑧ 持仓部分估值透明化**：持仓 AI 快照带 quote_failed_count+valuation_note（行情缺失仓位禁虚构现价、不强行割/守/补三选一——禁骑墙纪律只约束有行情的仓）；前端市值卡显示「已定价 X/Y 笔」；页面副标题去掉「实时盈亏」措辞。
  - **⑨ 数值核验来源区分**（ev2 items 增 origin）：labeledValue/evidenceItem 加 Origin——空=数据快照佐证、plan=模型自身计划价/公式输出、user=用户设定阈值；matchLabeled **快照来源优先命中**（同一数字快照与计划价都含时按被数据佐证记）；前端明细对 plan 项标「AI 计划价」并声明「合法复述但非快照佐证」。前端「置信度 N」改「AI 自评 N」（防与综合置信并列被当胜率）。
  - **本批遗留（P1 未修，动手前从这里挑）**：批量 AI 快照（市场/板块/自选/持仓）compact 时丢逐项行情 DataTime（analysis_context）；marketwide/factortable 新鲜度只与库内自身最大日期比较（整库落后仍判新鲜，应对照应有交易日）；newsai 情绪当日首算后冻结、公告有旧记录不按需刷新、龙虎榜/人气/盘中因子只取库内最大日期；管理端缺数据健康总览与手动补跑入口；日报核心块数据水位检查（指数/涨跌家数/资金流缺失时不得称完整）；Breadth/资金流/指数聚合接口 DataTime=采集时刻语义未拆分（前端已改「采集于」措辞）。

- **LLM 准确性 P0 第一批（P0-1 ac1 契约 + P0-7 机读拒答码，2026-07-21）防回归**（权威计划 `docs/LLM_ACCURACY_OPTIMIZATION_PLAN.md` §7.1，改 ai_client/llm_contract/拒答文案前先读）：
  - **① ac1 契约出口注入**（`llm_contract.go` applyAccuracyContract）：只在 `chatCompletion`/`chatCompletionStream` 两个**公开出口**各调一次（内部 chatCompletionInner 复用 streamInner 不会二次注入），且必须在审计 defer 注册**之前**。原 system/developer 内容收进 ac1 下方的「受限业务任务段」，上游只收到一个 system 信封，防后续同级 system 结构性覆盖；自定义模板正文不改，P0-6 仍负责模板 hash/迁移/L0-L3 完整分层。`llm.go` 探针（module=test）直发 HTTP 不走这两个出口，**永不注入业务契约**（探针通过≠业务推理可用）。改 `llmAccuracyContractText` 措辞必须递增版本（ac1→ac2）。
  - **② 温度与次数语义**：Repair=true 恒 0（无条件）；JSONMode 且非 repair 钳 ≤0.2（llmStructuredTempCap）；自由文本（qa/compare，JSONMode=false）不钳。**新增 repair 循环必须在代码 attempt>0 时置 `chatParams.Repair`**（现有七处：analysis/recommendation/dailyreport/screener_ai/recbear/**analysis_review/rec_review**），漏标=repair 轮跑高温。`repair_count` 是首轮后的额外次数、`attempt_count=1+repair_count`；达到模块上限即拒答或确定性降级，禁止隐式追加（`TestRepairOverLimitStops` 锁定总调用数）。P0-2 对外 manifest 的 `attempt` 采用 1 基序号。
  - **③ 流式/整包完整性门禁语义**：chat SSE 正常 EOF 无 [DONE]/finish_reason=拒收（eof_without_marker）；finish_reason 规范化后只允许 `stop/tool_calls`，`length/max_tokens/content_filter/failed/cancelled/未知枚举` 全拒；完整 JSON 兼容缺失 finish_reason。**Responses 严格仅 completed 成功**：流式必须是 response.completed/response.done 事件且其真实 response.status=completed，裸 [DONE]、空/failed/incomplete/冲突状态均拒。坏 JSON、缺必要 choices/type、流内/HTTP 200 error 包络不得静默跳过；Chat `message/delta.refusal`、Responses `type=refusal`/`response.refusal.*` 均归 `llm_content_filtered`；failed/cancelled 归 `llm_call_failed`，截断/缺终态归 `llm_response_incomplete`。整包 ReadAll 中断或超过 1MB 上限不得把可解析前缀当成功。`chatResult.FinishReason` 保留原始值（chat=finish_reason / responses=status），P0-2 元数据将消费它。
  - **④ flag 回滚开关**：`llm_accuracy_contract` **缺省开**（`!= "false"` 语义），管理后台「LLM 准确性契约」卡可关——关只回退①②③的 P0-1 行为；开关在每次公开调用出口只捕获一次，响应期间切换只影响**下一次**调用，禁止当前流中途 fail-open/误切严格模式。P0-7 stale/PIT、日历、新闻窗口和拒答码是常开安全地板，绝不随 flag 关闭（反例同时锁 daily/news/compare）。上游网关兼容性误杀时先关 flag 排查，再通过 P0-5 capability 显式声明兼容，不得默认放行未知终态。
  - **⑤ RefusalError 与前端传递**：code 覆盖 stale_quote/market_closed/market_calendar_unknown/report_window_not_open/report_processing/insufficient_fresh_quotes/quota_exhausted/quota_unavailable/llm_unavailable/llm_call_failed/llm_response_incomplete/llm_content_filtered；`llm_unavailable` 只表示配置不可用，网络/失败/截断/过滤不得混用。标准包络、QA NDJSON、compare 结果保留 code；Compare 必须先判 fresh<2 再解析 LLM 配置/配额，数据不足码不得被配置状态抢占；Analysis/Qa 按 stale_quote 分支，只有个股标准分析可提供历史解释重试，panel 直接展示拒答，中文关键词仅作为旧后端兜底；Compare 展示 ai_refusal_code。模块编排必须 `return callErr` 或 `%w` 保留错误链，严禁 `errors.New(callErr.Error())` 重建丢码。新增场景先在 llm_contract.go 登记常量并补反例。
  - **⑥ news 注入/聚合窗口**：`latestNewsBriefs` 使用闭区间 `[now-7d, now]`，未来记录和窗口外旧闻不注入；个股当日情绪聚合也只查 `[dayStart,min(dayEnd,now))`，不得让未来记录污染推荐并冻结缓存。东财时间解析失败直接丢弃，禁止回填 time.Now 伪装新闻；Time 带年份防跨年误判。日报当日新闻/提醒查询的结束时间取 min(dayEnd, now)，窗口内无新闻走既有 fallback，**别为凑条数放宽窗口**。
  - **⑦ 测试兼容**：assert 上游请求体的测试注意 ac1 会 prepend 首条 system（Contains 断言不受影响、精确断言会炸——TestResponsesCompletion 的 instructions 已改 HasPrefix+Contains）；依赖 prompt 字符数的精确断言（estimateUsage 粗估）应 `setContractFlag(t, false)` 关掉契约再测；切 flag 的测试必须 Cleanup 恢复 true（options 表写库，内存库共享）。repair 上限反例除分析主链路外，还要锁 analysis_review/rec_review 各自总调用数=2、第二轮温度=0；时间窗口测试使用固定时钟，禁止把新闻时间与严格 `< now` 上界放在同一精度边界制造 flaky。
  - **⑧ LLM 调用审计正文保持原文**：`llm_call_logs` 的 RequestBody/ResponseBody **原样保留**供管理员排障（仅 60KB 长度截断）；API key 走 Authorization 头不进 body。产品决策只取消 P0-8 的正文脱敏要求，勿再引入打码/哈希替换；P0-8 的 run/trace/业务结果关联及 schema/prompt/data/attempt/repair/finish 元数据未取消，随 P0-2 实施。
  - **⑨ 日报 PIT 日期/时点门**：正式日报只接受 trading_calendar 明确 open；closed 返回 market_closed，缺行/查询失败返回 market_calendar_unknown，禁止工作日近似。快照指数保留 data_time/trade_date、breadth/fund-flow 保留 trade_date/captured_at；核心块业务日期未知/非报告日，或同日指数源时间早于 14:30 时，必须从 LLM 输入和 evidence 值域剥离价格/涨跌/资金数值，只留来源/时点/stale_note；旧 breadth 不得驱动明日策略；自选异动必须 QuoteOK 且 freshness=fresh。

- **LLM 准确性 P0 第二批（P0-2 统一运行元数据 + P0-8 调用关联/审计完整性，2026-07-21）防回归**（权威计划 `docs/LLM_ACCURACY_OPTIMIZATION_PLAN.md` §5.1/§7.1 v1.6，改 llm_run/llm_call_log/任何 LLM 调用点前先读）：
  - **① 关联语义不可漂移**：trace_id=一个业务结果（分析记录/推荐批次/日报/问答会话/一次对比）；run_id=一个逻辑调用组——**主调与其全部 repair 轮共享同一 run_id，靠 1 基 attempt 区分**；复核/反方/交易计划各是独立 run，parent_run_id 回指主调；`attempt_count=1+repair_count` 不变式由 `llmRun.manifest()` 保证（TestLLMRunManifestInvariant 锁定）。业务表落 trace_id+llm_run_json、llm_call_logs 落逐请求元数据，两侧凭 trace_id 双向查——**新增任何 `chatCompletion*` 调用必须经 `newLLMRun`+`run.chatMeta(attempt)`+`run.record(res,err)` 三件套接线**，直填裸 `chatMeta{}` 只允许 llm.go 探针（module=test，有意独立）与单测。
  - **② structured_method 记「实际生效」形态**：JSON mode 因端点不支持回落时，chat/responses×流式/非流式**四处回落点**都调 `markJSONModeDropped()`——新增回落分支必须同步补调，否则审计把 free_text 冒充 json_object。manifest 里的 structured_method 是请求口径，逐请求真值以 llm_call_logs 为准（注释已声明，别把两处混为一谈）。
  - **③ finish_state 规范化枚举封闭**：只允许 stop/tool_calls/completed/length/max_tokens/content_filter/failed/cancelled/eof_without_marker/error/unknown/空（成功但上游未报告）；provider 原始值恒存 finish_state_raw。错误路径 res 常为 nil，`normalizeLLMFinishState` 从门禁文案还原 length/max_tokens/eof 语义——**改 llm_contract.go 的拒收文案（finish_reason=%s / eof_without_marker / max_output_tokens 字样）须同步核对该还原逻辑与其表驱动测试**。
  - **④ prompt/data hash 语义**：prompt_hash=初始业务消息序列（ac1 注入前）的 sha256，repair 轮不重算（同 run 恒等，测试锁定）；data_hash=喂给模型的数据快照 JSON。ID 用 crypto/rand 128bit hex（t/r 前缀），**禁改成时间戳/自增等可预测形态**（trace_id 透出前端响应）。
  - **⑤ 兼容边界**：全部新列 nullable 兼容零值，旧记录/旧会话读取不回填；QA 旧会话首次新提问才补写 trace_id（补写失败不阻断提问）；日报双败回滚路径不覆盖旧报告字段（失败尝试凭 trace 在审计侧可查）；推荐 buildPool 阶段失败（零 LLM 调用）只有批次 trace 无 manifest 属正常。manifest 的 coverage 字段预留恒省略（P1-1 填充），**不得在 P0-2 语义下伪造 coverage 数值**。列表查询新列已加入 Select 白名单，llm_run_json/正文重字段仍不进列表。
  - **⑥ 正文策略延续**：审计正文原样保留、仅 60KB 截断，P0-8 只补关联/完整性元数据——**严禁借本批结构重新引入正文脱敏**（用户明确决策）。管理端 llm-calls 的 trace 筛选同时匹配 trace_id 或 run_id。
  - **⑦ 测试时刻依赖教训**：TestStrategySignalHits 原只钉 bar 末日进日历，16:30 后 `wideExpectedDate` 切「今日」→ openDaysBehind 判 -1 被 fail-closed 拒绝——**下午运行必挂**。凡 seed 旧日期数据又依赖「期望交易日」的测试，日历必须同时钉 bar 末日与今日（两行齐钉，任意时刻确定性通过）。

- **LLM 准确性 P0 第三批（P0-3 字段路径证据链 ev4 + P0-4 跨模块 semantic validator，2026-07-21）防回归**（权威计划 `docs/LLM_ACCURACY_OPTIMIZATION_PLAN.md` §5.2/§7.1 v1.7，改 trust.go/llm_semantic_validator.go/快照 builder/各模块 parse 回调前先读）：
  - **① ev4 证据链是程序推导不是模型自报**：`evidence_id`（ev-001…按命中序分配）/`source`/`as_of`/`path` 全部由核验引擎（trust.go verifyEvidenceLabeled+matchLabeled）从快照值域回填——**禁止让模型输出 evidence_id 并照单采信**（模型侧 claims/evidence_ids schema 属 P1-2，另行设计防伪造）。source 提示在 `stockFieldHints`：quote/valuation 从快照元数据逐次读取，technicals=daily_bars、finance=eastmoney_f10、org_view=eastmoney_datacenter 是结构性事实（旧落库快照同样适用）；新增快照数据段时**必须同步补 source 提示**，否则该段证据链缺 source 维。
  - **② unknowns 语义**：`snap["unknowns"]`（field_path/reason/impact）只收「本应有但取不到」的段（个股：估值取失败/日线缺/财务缺/机构观点缺；as_of 快照：五段固定清单）；**已知不适用（ETF 无估值/财务）与有 fallback 声明的段（news 无相关新闻带 market_signals）不算缺口**——把「已知为空」写成 unknown 会稀释缺口警示。unknowns 随快照进 prompt（改变 data_hash 属预期）；`snapshotUnknownItems` 兼容原生与 JSON 回灌两形态（QA 复用落库快照）。旧记录无 unknowns/evidence_id/source/key_section 字段，前端 TrustBadges 全部 v-if 兜底。
  - **③ key_section 口径**：关键结论段=分析/日报「总结」、问答「回答」、对比「AI点评」（调用方 markKeySection 指定）；SnapshotMatched 只认 origin 空的命中（plan/user/context 复述不算）；Total>0 且 SnapshotMatched=0 时置信依据与前端 tooltip 点名但**不额外降档**（全局命中率已由 evidenceConfidenceSignal 管，双重扣分会放大单一信号）。
  - **④ P0-4 失败语义与既有路径对齐，不得静默修正**：分析/panel 的「block ⇒ 不得 bullish」在 parse 回调返回 error → 触发 repair（错误文案即修复反馈，含「语义校验失败」字样）→ 打满 1+maxRepairAttempts 仍不过 → 既有 degraded 落库（原文保留，非 success）；交易计划语义错误 → repair → 仍不过 → 既有 best-effort 不带 trade_plan；推荐短线 buy RR<1.5 → **透明降级** watch+risks 注记（沿 shortPlanPricesValid 先例：价位关系合法故保留价位，只降 action）。**三种处理形态是有意差异**（强语义拒绝 vs 单条目透明降级），改动前先想清楚落在哪一类。
  - **⑤ validator 是收口不是替换**：validateTradePlanSemantics 内部先调既有 validateTradePlan（**恒开，不受 flag 控**），再叠 flag 控的跨字段上下文反证（block/评级偏空——前置确定性 NoPlan 已拦，这里是防调用顺序重构旁路的收口）；normalizePick 既有归一化/剥除/悬空清零全部保留，applyRecPickSemantics 只在其尾部叠加。**影子纪律不动**：regime/bear/quality 影子门控仍不改写 action，RR 纪律与影子门控无关（它是价位赔率的确定性数学，非预测质量门控）。量化降级 picks 恒 watch，天然不受 RR 纪律影响。
  - **⑥ flag 边界**：`llm_evidence_refs`/`llm_semantic_validator` 均 `!= "false"` 缺省开（setting/system.go 同款惯例），管理后台「LLM 准确性契约」卡三开关并列；关闭只回退本批新增（ev_refs 关=不注入 unknowns 回 ev3 行为；validator 关=新增跨字段规则跳过），**P0-7 安全地板与各模块既有校验永不受这两个 flag 控制**。测试切 flag 必须 setupTestDB+Cleanup 恢复 true（options 表写库，内存库共享——setContractFlag 同款）。
  - **⑦ 版本与兼容**：核验引擎版本 ev3→**ev4**（断言 Version 的测试已同步）；prompt 版本不变（p17/d3/q10 等——unknowns 是 L2 数据段非提示词文本）；分析/panel/trade_plan schema_version 不变（新增字段全 omitempty 旧读兼容）。快照注入 unknowns 后 data_hash 与旧批次不可比属预期，勿当回归。

- **LLM 准确性 P0 第四批（P0-5 capability matrix + P0-9 模块化输出预算与截断语义，2026-07-22）防回归**（权威计划 `docs/LLM_ACCURACY_OPTIMIZATION_PLAN.md` §7.1 v1.8，改 model_capabilities.go/llm_budget.go/ai_client 回落点/任何模块调用参数前先读）：
  - **① 能力矩阵是「声明+观察」两层，观察优先**（`model_capabilities.go`）：内置声明只有 openai 全 supported，任意兼容中转缺省 json_object=unknown——**别往 builtinProviderCapabilities 里按 provider 名猜能力**（provider 是用户自由填写的字符串，静态表断言不了真实网关）；运行时观察 key=配置身份(configID)|model|endpoint（无配置身份退 URL 形态，配置改模型 key 天然失效），TTL 12h——**别去掉 TTL**（单次 4xx 误判会把配置永久降级 free_text，到期恢复 unknown 由隐式回落兜底重新探测）。
  - **② 四回落点必须走 noteJSONModeUnsupported**：chat/responses×流式/非流式的 JSON mode 隐式回落点已统一改调 `noteJSONModeUnsupported`（= markJSONModeDropped 审计观测 + 写能力观察 + 状态变化 SysLog）——新增回落分支裸调 markJSONModeDropped 会让声明化路由学不到该目标，回到每次在线试错。声明化路由 `applyCapabilityRouting` 只挂在 chatCompletion/chatCompletionStream 两个公开出口、且必须在 effectiveJSONMode 指针初始化之后（否则审计 structured_method 记不到 free_text）、在 applyAccuracyContract 之后（结构化任务被路由到 free_text 仍保留低温钳制）。
  - **③ 路由只消费 unsupported**：capabilitiesFor 的 JSONObject=supported/unknown 都照常发结构化请求；smoke 探到 supported 只是记录，**不解锁任何业务门**（探针通过≠业务可用不变式）。llm.go 探针纪律不变：module=test 直发 HTTP 不走 chatCompletion 出口、不注业务 prompt——新增的 JSON 结构化 smoke 与端点观察同样遵守（探测消息是固定最小文本）；smoke 非结论性失败（网络/5xx）不落观察。
  - **④ flag 边界**：`llm_capability_routing` 缺省开（!= "false"，setting/system.go 同款惯例），管理后台「LLM 准确性契约」卡第四开关；关闭只回退声明化路由，**能力观察记录与隐式回落不受 flag 控**。测试切 flag 用 setCapRoutingFlag（setupTestDB+Cleanup 恢复 true）；测试触发过观察的要 resetLLMCapabilityStore 清场（进程级 sync.Map 跨测试存活）。
  - **⑤ 模块预算表是唯一权威**（`llm_budget.go`）：模块 MaxTokens/RepairAttempts/RepairFeedChars 全部查表（moduleTokenCap/moduleRepairAttempts/moduleRepairFeed），**新增 chatCompletion\* 调用模块必须先在 llmModuleBudgets 登记**（TestModuleBudgetTable 锁 11 个业务模块全登记且预算>0）；旧常量 recMaxTokensCap/recReviewTokensCap/recBearTokensCap/reportReviewTokensCap/maxRepairAttempts/recRepairAttempts/parseStrategyRepairMax 已删，别复活平行常量。预算语义=上限声明（capModuleTokens 与用户配置取小），**调小前想清楚：过小触发 finish_reason=length 被契约门禁拒收，等于把该模块打挂**；qa 8000/analysis 4000 是给足的初值，部署后按 length 拒收率校准。
  - **⑥ repair 次数语义**：默认 1（llmDefaultRepairAttempts）；analysis/trade_plan 显式覆盖 2 是**保留既有行为**的声明不是新增预算——共用 AnalysisService.callWithRepair，按 run.Module 查表；dailyreport callReview 是硬编码「首轮+1 repair」结构，与表声明 1 一致，改次数须连同预算表同步。repair 回灌一律 moduleRepairFeed 截断（analysis/analysis_review/rec_review/screener 原先整段回灌已收口）。
  - **⑦ 第 13 个拒答码 llm_output_invalid**：语义=调用成功但 repair 打满仍无合法结构化输出（与 llm_response_incomplete 的「传输/截断不完整」区分）；接线出口 screener_parse 报错/daily 复盘解析失败/newsai 解析失败；**analysis/recommendation 打满走既有 degraded 落库不报错**（P0-4 第三批语义），仅 run.DegradedReason 同名标注——别把 degraded 路径改成报错。manifest 新增 output_budget=模块声明预算（非实际请求值），0 省略。

- **LLM 准确性 P0 第五批（P0-6 prompt 不可变快照与 L0-L3 分层，P0 收官，2026-07-22）防回归**（权威计划 `docs/LLM_ACCURACY_OPTIMIZATION_PLAN.md` §5.4/§7.1 v1.9，改 prompt.go/四模块系统提示常量/Prompts.vue 前先读）：
  - **① 拆分常量的编译期恒等不变式**：recommend/daily/qa/review 四模块默认整段（recRoleIntro/dailyReviewSystem/qaRoleIntro/analysisReviewSystem）= 「任务段常量 + 契约常量」的 **const 编译期拼接，与拆分前逐字节一致**——默认路径零行为变化、版本号（p17/d3/q10/p11 系）不递增。改任一段文本时必须意识到默认整段随之变化（此时才递增版本号）；**严禁把 schema/纪律挪进任务段常量**（自定义会把任务段整个换掉，TestPromptLayeredCompose 锁契约段必含输出协议关键词）。daily 的拼接无分隔符（「。规则：」直连）、review 是 "\n"、qa/recommend 是 "\n\n"——调整边界时先核对原文换行形态。
  - **② 自定义=L3 任务段注入，契约不可覆盖**：四模块消费点（rec buildMessages/dailyReviewSystemFor/qa buildMessages/analysisReviewSystemFor）的 custom 分支一律走 `composeCustomTaskPrompt(custom, 契约常量)`——用户段在前、`promptContractHeader` 分界、契约在后（后置+「冲突以本契约为准」声明压住上文）。**新增可自定义模块必须同步在 promptModuleContracts 登记契约段**，否则 composeCustomTaskPrompt 收到空契约=回到整段替换旧病。分析 5 模块结构天然分层（analysisRoleIntro/analysisOutputSpec 由 analysisSystemPrompt 组装恒在），不在 contracts 表属有意。存量「整段替换」模板自动降级为任务段注入是**有界面提示的显式变更**（Prompts.vue tip+契约展示区），不是静默——别为「兼容旧模板」加回整段替换分支。
  - **③ 版本归因语义**：`promptVersionFor` 产出 `base-custom.<hash8>`（hash8=content_hash 前 8 位，对**渲染前原始内容**计算——渲染后含变量值会让同一模板不同标的 hash 漂移）；同名版本必对应同一内容，改内容必换版本串。**业务表 PromptVersion 列已扩 32、llm_call_logs 32**——新增版本后缀形态先算长度（base 最长 sp1/p17 等 3~4 字符 + "-custom." 8 + hash 8 = 19~20）。分析主记录版本**只归因本模块 guidance 模板**；review 模板归因在 manifest 里 review run 自己的 prompt_version（analysisReviewSystemFor 返回）——别把「任一模板启用主记录就标 -custom」的旧粗粒度语义改回来（manifest 已提供精确归因）。
  - **④ revision/快照不变式**：内容变化才递增 revision 并落 prompt_template_revisions 快照行（同内容重复保存只切 enabled，不涨 revision 不落快照——TestPromptContentHashAndRevision 锁定）；递增基准=max(模板行 revision, 快照表 MAX(revision))+1，**别改回只看模板行字段**（升级前旧行/手工改库失步会撞 (template_id,revision) 唯一索引，测试里就是这么炸出来的）；快照行一旦写入不更新，删除模板不级联删快照（审计归因链）。升级前旧行 content_hash 空：读取侧（promptVersionFor）现算兼容，首次再保存时补建归因。Upsert 并发双建从 OnConflict 改为事务+唯一索引兜底（冲突报错重试），**别改回 OnConflict 无脑覆盖**——它丢失旧内容无法判定 revision 递增。
  - **⑤ lint 是诊断不是闸门**：lintPromptContent 只出 warnings 不阻断保存；运行时 renderPromptTemplate 宽容渲染（未知占位符原样保留）不变——把 lint 升级成保存阻断会打破「模板写错不至于让提示词失效」的既有契约。分层模块检测「疑似自带输出格式」只是提示重复，系统契约反正会追加（重复无害但浪费预算）。
  - **⑥ 前端/接口形态**：Upsert 响应从裸 template 改为 `{template, warnings}`（controller/api/prompt.ts 同步）；`Modules()` 的 default 已改为**任务段**（整段会诱导用户「以默认为模板」拷入契约造成重复），Contract 字段供展示——改 Modules 返回时两处语义别混。分析 5 模块 Contract 为空属有意（tip 文案已声明其身份总则恒由系统保证）。

## 4. 未完成项与储备（按数据源可得性推进）

> **2026-07-06 起后续开发按 `DEVELOPMENT_PLAN.md` 的批次（N1→N2→F1→T1→S1→F2→M1→M2→M3）推进**——它是面向执行的施工图（每批含方案锚点/依赖/验收）；分析依据与上游接口速查表在 `REFERENCE_ANALYSIS.md`。以下原有储备多数已并入该计划：

- 新闻情绪（→ 批次 N1/N2，财联社/东财源已调研齐）、财务详情/财报日历（→ 批次 F1/F2，东财 datacenter 网关免 Tushare）、回测模块（→ 批次 M2 时光机）。
- 多数据源系统级切换管理端（`data_source_configs` 接读写）。
- 东财 clist 全市场扫描候选源（B 档限流，储备；「不热」来源已由新浪 Market_Center 方向参数（跌幅/低PB 升序榜）覆盖大半，引入 clist 的边际收益降低）。
- 分析历史 rating 命中率统计（信任闭环二期）。
- 多模型交叉观点、研究证据包、财报季工作流、组合风格暴露分析（低优储备）。

## 5. 欠人工验证清单

- 推荐页/设置页新 UI 浏览器目验（筛选表单交互、真实生成一次、候选池全景、信任徽章 tooltip、AI 复核开关，亮/暗主题各一）。
- 指数 ETF 页与各 AI 页新增信任徽章/透明面板的浏览器目验。
- 6 主题目验欠账（至少 1 亮 1 暗，含 /thesis /notes /daily-report /stocks 详情 /etf）。
- 手机浏览器真机目验（≤768px 适配）。
- S1 欠目验：问答页流式渲染（首 chunk <300ms/markdown 排版/光标动画/核验徽章后置出现）、分析页与问答页风险闸门标签（找一只 ST 或一字板标的目验 block/warn 展示与 prompt 生效）、设置页总投资资金保存回填、`GET /api/admin/datasources` 健康端点真实数据；断路器 push2delay 降级须部署环境观察日志（本机 push2his 长期 EOF 正好是真实场景）。
- F2 详情页「财务摘要」块：真实数据展示（首次访问触发拉取，600519 等大票秒级可见）、柱线图 6 主题+移动端、长线推荐真实生成一次看 fin 字段与 ROE/增速 Bonus/evidence 引用财务数字且核验通过。
- T1 详情页 MACD/BOLL 副图与筹码峰卡：6 主题（副图 legend/双 grid 十字光标）+ 移动端单列布局欠人工目验；东财日线 f61 换手率与 lmt=210 真实拉取欠部署环境冒烟（本机 push2his 被限流，`LIVE_MARKET=1 go test ./datasource/ -run LiveMarket`）；推荐生成一次看 factors 新字段（rsi_14/macd_*/chip_profit）与 Bonus 新加分项实际出现。
- M1 第一部分欠部署验收：历史初始化断点续传真实跑通（本机 push2his 被限流拉不了 kline，部署环境 `LIVE_WIDE=1 go test ./service/ -run TestLiveWide -timeout 15m`，或直接管理端 `POST /api/admin/market/wide-init` 后 `GET /wide-status` 观察 done 推进/暂停恢复）；每日 16:10 job 首轮全量建库（5535 只×250 根约 40 分钟）；除权股真实命中观察（除权日看日志「检测到除权/送转」与 states.adjust_epoch，历史窗口衔接无断层）。
- M1 第二部分欠部署验收：/screener 选股页 6 主题+移动端目验（策略卡/命中表/条件编辑器弹窗）；全市场日线就绪后真实扫描一次——宽表**含 DB 读**的总构建耗时核对 <5s 量级（因子计算部分 5150 只已实测 1.43s，日志有「因子宽表构建完成」耗时明细）、内置策略命中数量合理性与命中原因人话展示；推荐真实生成一次看候选池全景出现「策略信号」来源。
- M2 欠部署验收：/backtest 回测页 6 主题+移动端目验（双 tab/统计卡/逐日表/α 直方图）；全市场日线就绪后真实回测一次内置策略——核对总耗时（DB 流式读+全市场逐日因子，预期 5~15s 量级，结果带 elapsed_ms）、命中与成交数量合理性、复权自洽剔除计数（adjust_suspect 大量出现说明除权重锚有漏）；推荐批次回验真实批次看 α 分布；分析页发起一次回溯分析（as_of 取 1 个月前）看快照缺失声明与「回溯校验」弹窗节点收益。
- M3a 欠部署验收：详情页「主力资金」卡与「龙虎榜上榜记录」卡 6 主题+移动端目验（找近期上榜股如实测的华天科技类标的）；部署环境交易日盘后观察 16:35/18:45 两轮 job 日志（涨停池聚合/人气榜/龙虎榜行数+首轮近 30 天回填）；次日看市场分析 mood 段与日报「连板/炸板情绪段」真实出现且核验通过；推荐真实生成一次看 Bonus 出现「龙虎榜机构净买入/人气榜/主力资金连续净流入」加分与 factors.main_net_days 字段。
- M3b 欠部署验收：部署环境交易日 17:05 观察盘中因子同步 job 日志——全市场实测耗时核对 2~4 分钟量级（8 worker×80ms 节流，5150 只）、落库行数接近当日交易股数、「累计 N 个交易日」计数逐日 +1；短线推荐真实生成一次看 Bonus 出现盘中因子加分项（「尾盘30分放量拉升/收盘强于全天均价/午后重心上移」等）与 factors.intraday_date/tail30_chg 字段且证据核验通过；连续几日后 intraday_factor_dailies 表按日增长无重复（先删后插幂等）。

- M3c 欠部署验收：个股分析真实发起一次看「交易计划」卡出现（买入区间/目标/止损/周期/盈亏比/仓位 tooltip 公式因子/操作清单），偏空标的与 ST 标的确认 no_plan 原因如实展示；Prompts 页 9 模块列表与占位符提示 6 主题目验，改推荐/日报模板后立即生成核对即时生效且记录 prompt_version 带 -custom；/heatmap 热力图 6 主题+移动端目验（行业/概念切换、点击进板块详情、树图文字色随主题），板块详情页 K 线与成分股表（龙头/领涨 tag）目验——板块指数日线走 push2his 本机限流，部署环境跑 `LIVE_BOARD=1 go test ./datasource/ -run LiveBoard`。

- 2026-07 杂项批欠部署验收：管理后台三张新卡片目验（新闻采集间隔+自动 LLM 开关、LLM 回退开关+指定配置下拉、注册策略原有卡回归）；`/admin/llm-calls` 调用记录页目验（筛选用户/模块/状态、分页、行点击详情弹窗请求响应全文、6 主题+移动端）；无 LLM 配置的普通用户真实发起一次分析验证回退命中（配额记本人、审计行归属正确）；`endpoint_type=responses` 配置对真实上游（OpenAI 或 new-api 中转）测试连接+问答冒烟；推荐设置页股价默认 ≤50 生效（老用户偏好里存过 10 的须自行改一次）。

- P3a 欠部署验收：详情页「机构观点」块真实数据目验（600519 类研报密集股首次访问秒级可见：评级分布/最近评级变动/目标价中位与偏离、002230 类调研密集股看调研列表；6 主题+移动端）；个股分析真实发起一次看 org_view 段出现且核验通过（prompt_version=p13）、交易计划 plan_note 在机构目标价偏离大时出现对照说明；问答冒烟一次（q8）确认引用机构数字核验通过。

- P3b 欠部署验收：板块详情页「主力资金」「估值」两新卡 6 主题+移动端目验（半导体 BK1036 类活跃行业板块；概念板块确认估值卡不渲染）；部署环境交易日 16:10 增量后看「板块估值聚合完成」日志（落库约 496 行/日、未匹配行业跳过日志为空）与 board_valuation_dailies 逐日增长（hist_days 逐日 +1）；板块 AI 分析以行业板块名为 focus 真实发起一次，看 board_valuation/board_flow 两段出现且核验通过（prompt_version=p14），概念板块名确认两段如实缺席；`LIVE_BOARD=1 go test ./datasource/ -run TestLiveBoard` 冒烟（本批已扩板块清单+资金流两断言）。

- P3c 欠部署验收：/screener 条件编辑器弹窗「AI 生成」块 6 主题+移动端目验（textarea/生成按钮/预览 tag/unmatched 警示 tag/套用按钮）；真实 LLM 三类白话样例验收——「量比 2 以上放量突破 20 日新高」（动量放量）、「缩量回踩 20 日线且获利盘低于 15%」（缩量回踩）、「RSI 超卖且年内低位」（超跌）各解析一次，人话预览与意图一致、套用后试扫/保存正常；含无法映射表述（如「市盈率低于 20 且北向重仓」）确认 unmatched 警示如实出现不硬凑；配额页核对每次解析计 1 次。

- 异步任务化批（2026-07-14）欠部署验收：**这批就是为了修「上游 60s 超时」的线上问题，部署验证是核心**——①推荐页点「生成」应 2 秒内出现「生成中」横幅（历史列表同步出现「生成中」批次），期间刷新页面确认自动恢复跟踪、任务完成后自动展示；②真实上游观察单次 LLM 调用是否已压进 60s 窗口（`/admin/llm-calls` 看 recommendation/daily_report 模块的耗时列，新增「首块/总耗时」两值——**首块≈总耗时说明网关假流式**，此时唯一有效手段就是输出预算，如仍超时可再压 recMaxTokensCap）；③故意断网/等上游超时一次，确认推荐落出「量化降级」批次（卡片带「量化降级」标签、置信 low、规则价位）而非整批失败；④日报手动重生成一次成功覆盖、再模拟失败（临时改坏 LLM 配置）确认旧报告保留+错误注明「已保留原报告」；⑤追踪刷新 5 只批次实测耗时（条目并发 4 后应明显快于旧串行）不再触发前端超时；⑥重复快速点击「生成」确认只建一个任务、配额只计一次。

- 体验杂项批（2026-07-14）欠部署验收：①设置→偏好（或推荐页表单）开「排除创业板/科创板」后生成推荐，确认 30/68 前缀标的进「被筛除」清单并带原因回显；②今日待办 rec_review 条目点「已读」就地消项，2h 追踪刷新后确认不回弹未读；③收盘日报删除（正常报告可删、生成中 15min 内拒删有提示；删除不影响关联推荐批次）；④推荐结果卡点标的名/代码新窗口打开个股详情；⑤各 AI 展示处（推荐历史与结果头/分析元信息/问答会话头/日报头/对比 AI 点评/选股 AI 解析块/首页今日观点卡）出现「配置名 · 模型」标签，删除该 LLM 配置后确认回退显示 provider。

- 推荐准确性第一批（2026-07-16）欠部署验收：①生成一批推荐——结果头出现「市场状态：进攻/中性/防守」标签（悬停见判定依据明细），短线卡片价位区出现「建议仓位」（悬停见目标波动公式因子），卡片底部「挂止损提醒」一键创建成功并在提醒页可见；②确认 defense 档（若遇到）buy 条目**未被改写**（影子模式），`recommendation_candidate_events` 表出现 gate_type=regime_shadow 行；③交易日 16:10 增量后确认 `stock_universe_dailies` 逐日 +5500 行左右（含 industry/is_st/suspended）；④生成后查 `recommendation_labels` 出现该批次 池内候选数×5 行 pending，次日起 2h 追踪 job 日志出现「推荐标签结算完成」，horizon=1 标签率先 matured 且 entry=次日开盘价；⑤一键建仓后下轮追踪确认追踪块出现「实际收益（按你的买入价）」并列展示、labels 出现 entry_mode=actual_position 行；⑥历史表现卡出现「买入胜率（已成熟）」主指标与 watch/降级剔除计数；「错误归因报表」弹窗在成熟样本积累后（约 1~2 周）有分组数据；⑦自选池含无成交额数据的新股时确认其被流动性门槛拒绝（不再静默混入）。

- 推荐准确性第二批（S2 影子件，2026-07-16）欠部署验收：①开「AI 复核」生成推荐——「反方研究员」开关自动联动打开，buy 卡片出现「反方论证」块（高/中/低危 tag + bear case + 「影子：不改写结论」说明），action/置信度未被反方结论改写；关复核单开反方也可用；②high 危 buy 出现后查 `recommendation_candidate_events` 有 gate_type=bear_shadow 行（would_be_action=watch），gate_version=br1；③找一只数据残缺标的（长线财务缺失/次新股日线不足）确认卡片出现「数据质量（影子）：缺失 …，若门控转正置信度将封顶至 N（当前未封顶）」提示，置信度实际未变；当日无新闻标的出现「情绪数据缺失，不代表情绪中性」标注；④左栏「影子门控对照」弹窗打开——标记样本积累后（防守档/反方高危/数据缺失场景）按 gate_type 出现 gated vs ungated 两行对比与「若强制损失 buy N/M」覆盖率行，无标记时空态文案正常；⑤`/admin/llm-calls` 出现 module=rec_bear 调用记录，单次生成总调用次数 ≤3（主调+复核+反方）；⑥迁移后确认 `recommendation_reflections` 表已建（空表，LLM 反思延后）。

- 推荐准确性第三批（S3 前半，2026-07-16）欠部署验收：①交易日 16:10 增量后日志出现「因子快照落库完成: <日期>，~5150 行（累计 N 个交易日）」，`factor_snapshot_dailies` 逐日 +5150 行左右且**同日重跑不重复不覆盖**（累计天数逐日 +1）；`/api/screener/status` 的 snapshot_days 同步增长；②推荐页左栏「召回评估（好股票有没有进池）」弹窗——有已走完持有期（≥5 交易日）的成功批次后打开，出现 Recall@K 三档（进池/进名单/入选）、Top-K 去向分桶 tag、来源消融表、机会集 vs 候选池收益分布、批次明细；近期批次持有期未走完时空态/报错文案正常；耗时数秒级（看 elapsed_ms）；③管理员下拉菜单「因子 IC 排行」页——全市场日线就绪后点「重新计算」，数秒内出 19 因子 × 5/10/20 日窗口的 IC/ICIR/胜率排行（首批横截面受 250 根日线历史所限约 24 个），二次进入直接读缓存；④确认本批零 LLM 调用（`/admin/llm-calls` 无新增模块）、推荐生成行为与版本号完全不变。

- 推荐准确性第四批（S3 后半 walk-forward，2026-07-16）欠部署验收：①管理员下拉菜单「Walk-Forward 基线」页——全市场日线就绪后点「重新计算」，数十秒内（看耗时 meta，日志有「walk-forward 评估开始/完成」两条）出短线/长线两段：各出现「窗口已缩放」tag 与说明（250 根历史下短线约 {train≈160/val≈29/test≈19}、长线约 {train≈137/val≈25/test≈17} 各 1 折）、切分表（训练/验证/测试日期范围，段间隔=purge+embargo）、指标表（Precision_net@K 与 Precision_alpha@K 分开、净收益中位数与严重亏损率并列、验证/测试两段行）；②月度走查表出现最近约 11 个月 × 每策略行，展开行首见 Top10 组合成员（名称/代码/评分/净收益，近月成员标「未走完」属正常）；③二次进入直接读缓存不重算；④确认本批零 LLM 调用、推荐生成行为与版本号完全不变（`/admin/llm-calls` 无新增模块）；⑤6 主题+移动端目验新页（NDataTable 已带 scroll-x 横滚）。

- 推荐准确性一致性修复批（2026-07-17）欠部署验收：①**宇宙快照修复**（最重要）：交易日 16:10 增量后无「宇宙快照落库失败」WARN，`stock_universe_dailies` 开始逐日 +5500 行左右（此前因 pe_ttm 列名 bug 一行都没落过）；②标签结算：交易日晚间「推荐标签结算完成」日志正常，新生成推荐当晚**不再**被误判 skip_suspend（次日盘后才成熟或 skip）；已有的历史 skipped 标签中因该 bug 误判的无法自动恢复（终态），如需要可手工 `UPDATE recommendation_labels SET maturity_status='pending', skip_reason='' WHERE maturity_status='skipped' AND skip_reason='skip_suspend'` 让下一轮重结算；③新推荐批次的事件行带 gate_types（命中多门控时逗号多值）、质量门控明细 quality_gate_version=qg2 且带 data_age_trade_days；④归因弹窗头部出现「成熟样本 N（买入 M）」；⑤walk-forward/IC 页重新计算跑通（Top-K 机会集过滤+ST as-of 在快照积累前回退声明出现在 Notes）。

- 外部审查修复批（2026-07-17，d6609c7）欠部署验收：①**执行语义 l2**：交易日晚标签结算日志正常，新结算标签 label_version=l2、horizon=1 标签的 exit_date 为 entry_date 的次一交易日（T+1）；归因/影子报表 Notes 出现「仅统计 label_version=l2」声明（历史 l1 样本清零属预期，可选人工重算 SQL 见 §3 条目①）；②零推荐批次（若遇到拒选）事后查 `recommendation_candidate_events` 该批仍有全池事件行；`recommendation_batches.facts_recorded` 新批恒 true；③追踪：止盈/止损/过期条目收益不再随行情漂移（隔天再刷不变）；已平仓关联持仓的「实际收益」定格在卖出价；周末/节假日追踪刷新不再新增当日 bar；④首部署场景确认 `factor_snapshot_dailies` 只含 init done 标的（不再有 1 根 bar 的全 NaN 快照）；⑤前端：无 LLM 配置的普通用户可直接发起分析/问答/推荐（走管理员回退）；白话建策略/walk-forward 页长任务不再 20s 被掐断；快速切换个股详情无旧数据闪现；403/429 报错显示后端 message；凌晨保存逻辑卡日期不再退一天；⑥安全：改密后旧 refresh 全失效；CSV 导出打开 Excel 无公式执行；>2MiB 导入文件被前置拒绝；⑦通知点击「收盘日报」跳转正常（/daily-report）。

- LLM 准确性 P0 五批集中验收（2026-07-21~22，faac764/1076109/a297da1/c822275/877eb65/本批）：①**契约与拒答**：真实上游发起分析/问答/推荐各一次——`/admin/llm-calls` 详情确认请求首条 system 含 ac1 契约信封、结构化调用温度 ≤0.2；制造一次上游截断（临时把某模块预算改小）确认返回 `llm_response_incomplete` 拒答码而非半截内容落库；管理后台「LLM 准确性契约」卡四开关可见且缺省全开；②**运行元数据**：任一分析记录/推荐批次/日报详情带 trace_id，`/admin/llm-calls` 按 trace 筛出主调+复核/反方/交易计划各 run（parent 回指、repair 轮同 run 不同 attempt）；③**证据链 ev4**：分析详情「数值核验」明细带证据 ID（ev-001…）与来源维、「数据缺口」折叠区在估值/财务缺失标的上出现；④**capability 路由**：用兼容中转配置测试连接看 JSON 结构化探测结论并入消息；一次 json_object 回落后（llm-calls 里 structured_method=free_text）12h 内同目标后续调用直接 free_text（省一次注定失败请求）；⑤**预算**：llm-calls 各模块 max_tokens 符合预算表（qa 8000/analysis 4000 等）；⑥**P0-6 本批**：Prompts 页确认四个扩展模块出现「系统契约（自动追加）」只读区、默认任务段展示为角色行；自定义 daily 模板保存后看 lint 警告（故意写 `{{Date}}` 与「只输出 JSON」）、启用后生成日报确认输出仍是合法 JSON（契约段生效）且 `daily_reports.prompt_version` 形如 `d3-custom.xxxxxxxx`；改模板内容再生成确认 hash8 变化、`prompt_template_revisions` 逐版累积；旧模板用户（若有）确认界面提示与行为变化说明可见。

## 6. 技术风险（长期有效）

- **数据源**：免费源限流/字段变更是最大不确定性；必须显示数据更新时间，异常时降级而非编造。Tushare 分档按需启用（免费档/低 cost 档），高级档不实现。
- **LLM 幻觉**：结构化输出 Schema 校验 + 有限次 repair + 优雅降级；数据快照与 prompt 版本落库保证可复现；信任层核验结果透明展示而非静默修正。
- **合规**：大陆向不特定对象荐股属持牌业务；本项目定位**个人自用研究工具**，全产品统一「研究参考/交易计划/候选标的」表述，公开部署前须合规评估。
- **成本**：LLM 调用有缓存/配额熔断/限流/token 审计；高频页面不直调 LLM；后台任务失败落库防反复烧 token。

## 附：工程约束

- **go:embed 路径约束**：`go:embed` 不能引用包外/上层路径，前端产物构建时拷入 `server/web/dist`（Dockerfile `COPY --from=webbuilder`）；本地构建会改写 `server/web/dist/index.html`，**提交前在仓库根目录执行 `git checkout -- server/web/dist/index.html`**。
- 反代部署需配 `TRUSTED_PROXIES`，否则限流/日志拿不到真实 IP（详见 DEPLOYMENT.md）。
